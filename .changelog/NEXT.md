# Unreleased

- **A never-run cron task no longer fires an immediate "catch-up" for a scheduled slot that predates when the task was set up.** `shouldRunTask`'s cron catch-up bounded a never-run task only by "the occurrence before the most recent slot" — which is always earlier than that slot, so the guard was vacuous and *every* never-run cron task reported `cron-catch-up` for its most recent past slot. For long periods this was jarring: a weekly `branch-cleanup` scheduled `0 9 * * 0` (Sunday 09:00) that was enabled mid-week immediately showed as **due now (catch-up)** on the Schedule Timeline for *last* Sunday's slot — a slot that elapsed before the task existed and was never actually missed. Tasks now carry a `createdAt` timestamp (stamped when first configured; `loadSchedule` backfills existing installs to "now", which only suppresses catch-up for already-past slots and never affects future cadence), and never-run catch-up only fires for a slot that elapsed *after* `createdAt`. So a weekly task enabled on a Friday now waits for the coming Sunday instead of running immediately, while a task that was genuinely configured before a missed slot (daemon down / check window missed) still catches up as before.

## Security

- **[issue-2371] The autonomous Autofixer now repairs crashed apps in an isolated sandbox behind an explicit promotion gate — untrusted crash logs can no longer drive a credentialed agent against your live checkout.** The autofixer built a file-editing CLI prompt straight from raw PM2 stderr/stdout and ran it every 15 minutes in the managed repo with the full host environment inherited, so a payload reflected into a crashing process's log could become instructions to an agent with shell + file access and every ambient credential. It now: cuts a disposable, detached git worktree at HEAD and runs the agent there (a hard rollback boundary — no git checkout means no autonomous repair at all); hands the agent an allowlisted environment stripped of unrelated secrets (DB passwords, cloud keys, GitHub tokens, filesystem-backed service-account creds — only the direct AI-provider API-key vars the CLI reads survive); fences the logs as clearly-labelled UNTRUSTED evidence with a per-session, forge-resistant delimiter and "data, not instructions" framing; for the default Claude provider denies the shell/network toolset so the agent can only read/edit files; validates the resulting diff (size cap, rejects absolute/traversal paths and edits to `.env`/`.git`/`.github`/secrets, and blocks any diff that writes a credential — matched by pattern or by a live env secret value — into the repo, closing the read-then-promote exfil path); optionally runs a user-configured `verifyCommand` in the worktree; and only promotes the change to the live checkout + restarts the process when the user has explicitly enabled `settings.autofixer.autoPromote` (default **off** — otherwise the validated patch is staged beside the session for manual review), rolling the patch back if the post-apply restart fails. Adversarial tests cover fenced prompt-injection payloads and credential-access attempts. (Non-Claude CLI providers retain their own provider key in-env with network access, since PortOS can't portably constrain their toolset — the worktree + promotion gate + diff secret-scan still contain file changes; hardening that residual — e.g. hard-gating to constrainable providers or a network namespace — is a documented known limitation.)

- **[issue-2376] The CoS Schedule "Global Pause" help is now reachable by keyboard and touch.** Its explanation used to live in a CSS-only `group-hover` popover on a non-focusable info icon, so keyboard and touch users could never read what pausing does before toggling it. It now uses a new reusable, accessible `InfoTooltip` (`client/src/components/ui/InfoTooltip.jsx`): a focusable `<button>` trigger that reveals the help on hover, keyboard focus, OR click/tap, dismisses on Escape or outside click, and links the panel to the trigger via `role="tooltip"` + `aria-describedby` (the ARIA tooltip pattern). The compact inline layout and existing pause toggle are unchanged.

- **The reference-audio waveform is now keyboard-operable and two hand-rolled modals gained real dialog semantics.** The Songs "reference analysis" waveform scrubber was a mouse-only `<div onClick>` (`role="presentation"`) — keyboard and screen-reader users could not seek at all. It is now a proper `role="slider"` with `aria-valuemin/max/now/valuetext`, focusable, and scrubbable with the arrow keys (±1s, ±5s with Shift) plus Home/End (WCAG 2.1.1). Separately, the CyberCity photo-mode "postcard preview" overlay and the Catalog "pick from media gallery" picker were hand-rolled `fixed inset-0` backdrops with no `role="dialog"`, no `aria-modal`, no Escape-to-close, and no focus trap; both now render through the shared `Modal` (focus trap + Esc stack + labelled dialog), with the postcard preview portaled so it escapes the WebGL HUD's stacking/pointer-events context.

- **Modals and drawers now trap and restore keyboard focus, and toasts are announced to screen readers.** Opening any shared `Modal` or `Drawer` (which back the great majority of the app's dialogs, config drawers, and lightboxes) now moves focus into the dialog, keeps Tab/Shift+Tab cycling inside it instead of leaking to the page behind, and returns focus to the control that opened it on close (WCAG 2.4.3 / 2.1.2) — implemented once in a reusable `useFocusTrap` hook. The toast stack is now a labelled ARIA notification region: default toasts announce politely (`role="status"`) and error toasts assertively (`role="alert"`), with the decorative status glyphs hidden from assistive tech, so screen-reader users hear notifications (including error toasts) as they arrive.

## AI Providers

- **Creative Director LLM stages are now independently configurable.** AI Assignments has separate provider/model rows for treatment generation, production planning, and scene evaluation. Point **Scene evaluation vision model** at a local Ollama or LM Studio vision model to judge rendered scenes without using the system-default agent; treatment and planning can likewise be pinned to any agent-capable CLI/TUI provider.

- **Creative Director provider/model can now be overridden per project.** Each Creative Director project has a **Models** button (header of the project page, deep-linkable via `?models=1`) that opens a drawer to pin the treatment, production-plan, and scene-evaluation stages to a specific provider + model *for that project* — overriding the global AI Assignment. A stage left on **Inherit global** falls back to the global assignment (and then the system default), so the override is purely additive. The pin travels with the project record and federates to sync peers verbatim.

- **[issue-2336] xAI Grok is now a built-in AI provider — API, Grok Build CLI, and Grok Build TUI.** Three new providers ship ready to enable under AI Providers: **xAI Grok** (the OpenAI-compatible chat API at `https://api.x.ai/v1` — paste your xAI key and pick a model), **Grok Build CLI** (headless, for one-shot generations and file-writing CoS agents), and **Grok Build TUI** (the interactive `grok` coding agent driven over a PTY like Claude Code / Codex). All three arrive disabled by default (nothing calls Grok until you turn one on) and reach existing installs on upgrade. The CLI/TUI use the local `grok` binary; run `grok login` (or set your xAI credentials) to authenticate it.

- **Grok Build CLI/TUI no longer show a model picker — they use the binary's latest default, like Antigravity.** The CLI and TUI now store a `grok-configured-default` sentinel (same pattern as Codex / Antigravity), so provider/model dropdowns hide model selection and spawn paths omit `--model`, letting the local `grok` binary pick its current default. The xAI Grok **API** provider is unchanged and still lets you choose among chat models.

- **The Codex code reviewer now has a selectable model tier, like the Ollama/LM Studio reviewers.** AI Providers → Code Review Defaults gains a **Codex model** dropdown (shown only when `codex` is in the reviewer chain) populated from the Codex provider's model tiers (e.g. `gpt-5.6-luna` / `gpt-5.6-terra` / `gpt-5.6-sol`). Unlike the local-LLM reviewers — whose model the `/api/code-review/local` endpoint injects server-side — Codex is a CLI the review-loop follow-up agent invokes directly, so the chosen tier is threaded into the follow-up's prompt as `codex --model <id>`. Leave it blank to let the Codex CLI use its own default. The value persists to `settings.codeReview.codexModel` and rides along to the follow-up via `reviewLoopCodexModel`.

## Layered Intelligence

- **Layered Intelligence now runs as a visible agent you can watch — and talk to in a TUI — instead of an invisible in-process handler.** It used to be a "handler-backed" task: on each fire it made one inline reasoning call and filed a tracker issue with no agent record, so it never appeared in the CoS queue or the Active Agents page and couldn't be interacted with mid-run. It's now a normal agent-backed scheduled task with two programmatic slots around the agent — a pre-agent step that gathers the app's goals/telemetry/open issues and builds the reasoning prompt, and a post-agent step that validates the agent's structured output and files exactly one deduplicated tracker issue. The reasoning run shows up as an agent like any other automation, and when you select a TUI provider/model you can attach to its terminal and message it while it thinks (`btw`). The "reasoner never writes code" guarantee is preserved by isolation: the agent runs in a throwaway git worktree that is discarded without a commit, push, or merge, so its only durable output is the structured result the filing step consumes. This generalizes into a reusable "programmatic-I/O task" mechanism (pre-agent input hook + post-agent output hook) for future task types that need deterministic data collection or output processing around an agent. Existing installs inherit the throwaway-worktree posture automatically on upgrade (schedule defaults are backfilled) — no migration needed. Per-app enable/interval/provider/model and behavior config (sources, scopes, rules, hand-off) are unchanged, and the Edit App → Intelligence "Last run" status still reflects filed/parked/skipped/no-op outcomes.

- **[issue-2322] Layered Intelligence is now a per-app scheduled task, not a global sweep.** The self-improvement loop used to run as a single global "Layered Intelligence Loop" job that swept every enabled app on one daily fire, with a confusing split between a global on/off (CoS → System Tasks) and per-app config (Edit App → Intelligence) — a per-app card could read "Enabled · Due now" while nothing ran. It now runs like every other self-improvement automation: a per-app, deterministic, handler-backed task in CoS → Schedule with its own per-app enable, interval, provider/model, and "Run now". Enabling the `layered-intelligence` task in the Schedule (plus per app on the Intelligence tab) drives it; the reasoning model still only returns JSON and files one deduplicated tracker issue — no code, no agent. Per-app behavior (telemetry sources, allowed scopes, guidance rules, hand-off) still lives on the app's Intelligence tab. An install upgrade migrates each app's scheduling into its per-app task override and tombstones the old global job, preserving effective on/off state. Note: because it's now a self-improvement task, the loop runs only while CoS improvement is enabled.
  - The standalone `/layered-intelligence` page has been removed — with the loop now a per-app scheduled task, its status is seen and managed in CoS → Schedule like every other task, and per-app behavior stays on Edit App → Intelligence. (The dedicated cross-app overview/proposals page and its endpoints are gone; nothing else surfaced there.)
  - **[issue-2326] The app Automation page now edits per-task provider/model overrides inline.** Each task row on Edit App → Automation gained a "⚙ Configure" disclosure that expands a per-app provider + model override (with "Use default provider" to inherit the global/task default) — previously reachable only from CoS → Schedule → Configure. The `layered-intelligence` row additionally links straight to its behavior config (telemetry sources, allowed scopes, guidance rules) on the Edit App → Intelligence tab, so an app's full self-improvement setup is configurable without leaving the app.

## Changed

- **The Shell page's quick-launch shortcuts are simpler.** The one-click `codex` launcher now runs `codex --dangerously-bypass-approvals-and-sandbox`, so an interactive Codex session started from the Shell page can edit files and run commands without stopping to approve each one — matching how the `claude` shortcut already runs with `--dangerously-skip-permissions`. The `claude` shortcut no longer appends `--system-prompt .` (it launches with Claude's full default prompt), and the redundant `claude (full)` shortcut has been removed.

- **[issue-2372] The three Digital Twin evaluation-suite panels (Values-Alignment, Adversarial-Boundary, Multi-Turn) now share one lifecycle + presentation shell instead of each re-implementing ~315 lines.** The panels had drifted — Values-Alignment loaded its suite/history without the `{ silent: true }` option its siblings use, so a failed load double-toasted. That drift is fixed at the API layer (`getValuesAlignmentTests`/`getValuesAlignmentTestHistory` now accept options) and the shared load path always silences. The duplicated loading, multi-provider parallel run, persona-not-found handling, result expansion, reactive history prepend, and toast fan-out now live in a `useTwinEvaluationSuite` hook plus a `TwinEvaluationSuitePanel` presentation component; each panel is reduced to a small suite descriptor (copy + status semantics + api calls) and its one genuinely-different expanded-detail renderer. Every suite's observable copy and status fields (Aligned/Held/Consistent, score labels, per-run counts) are preserved unchanged. New hook-level behavior tests cover silenced loads, parallel runs, history prepend/cap, and the persona/partial-failure toast branches.

- **[issue-2373] The six data-import services now share one streaming-ZIP lifecycle helper instead of each re-implementing it.** browserHistory, spotify, youtube, whatsapp, takeoutLocation, and discord all repeated the same ZIP predicate plus a hand-rolled `createReadStream → parseZip` lifecycle (single-settle guard, source/parser teardown on early failure, autodrain of skipped members, per-entry promise collection, and close/error coordination). That is now centralized in `server/lib/zipStream.js` as `isZipUpload(file)` and `collectZipEntries(path, { match, onMatch, maxBytes? })` — which owns teardown, autodrain, and awaiting every matching entry's work — leaving each importer with only its domain match/parse callbacks. No behavior change; covered by new failure / truncated-archive / corrupt-member regression tests.

- **CoS scheduled tasks get a day-of-week + time-of-day picker — no more typing crontab syntax for weekly schedules.** Every CoS cron surface (Schedule tab overrides and global config, the Workflow/Timeline schedule editor's pinned-time mode, CoS system jobs, and per-app custom tasks) now leads with a row of day pills (S M T W T F S) plus a time input: click the days it should run and set the time, and the cron expression is built for you (no days selected = every day). The raw crontab field and presets are still there behind an "Advanced" toggle for interval/stepped schedules the picker can't represent, and it round-trips — an existing `0 9 * * 1,3` lights up Mon/Wed at 09:00. Built on a shared `WeekdayTimePicker` component and `parseSimpleCron`/`buildWeeklyCron` helpers so all surfaces stay consistent.

- **Creative Director is now a top-level page under Create, not a Media Gen tab.** It moved out of the Media Gen tab strip to its own `/creative-director` route with a Create sidebar link, so it's reachable directly (⌘K, voice nav, and the sidebar) without first opening Media Gen. Legacy `/media/creative-director` links and deep-links (including `:id/:tab`) redirect to the new route, preserving query string and hash.

## Added

- **CoS → Timeline: the Workflow tab is now a schedule timeline — see when every active task will run, and fix collisions in place.** The conceptual stage-pipeline visualization at `/cos/workflow` has been replaced with a real execution timeline over a 24-hour or 7-day window: cron launches at their exact slots, interval tasks/jobs at their learning-adjusted next runs and repeats, perpetual tasks as an open-ended draining band with reset/recheck markers, and a "Scheduled order" strip showing the next launches in sequence. Launches from different tasks within 15 minutes of each other get a collision ring plus a "tight handoffs" counter, so schedules can be staggered before they pile onto the concurrent-agent slots. Selecting any track (or an unpinned rotation/on-demand task) opens an inline editor to change its timing mode, frequency, enabled state, weekday-only flag, and hard `runAfter` dependencies without leaving the page — with the zoom window and open track deep-linked via URL params. Projections respect parked app-scoped perpetual tasks and weekday-only schedules (weekend slots that the scheduler would refuse are never drawn), and switching a system job between pinned-cron and interval modes now explicitly clears the inactive mode's fields.

- **[issue-2328] Auto-fix outcomes are now aggregated into dashboard telemetry with a success-rate trend.** The structured `metadata.diagnostics` persisted on every error-driven task (tier / category / failure reason) were queryable but never surfaced. A new pure aggregator (`server/services/autoFixMetrics.js`) reads them off task records and rolls them up by fallback tier, failure category, and task status — plus a time-to-recovery summary (count / avg / median / min / max) and a daily success-rate trend — exposed via `GET /api/autofix/metrics` and rendered by a new **Auto-Fix Telemetry** dashboard widget (overall recovery rate, trend sparkline, per-tier breakdown, median recovery time). To measure time-to-recovery, each diagnostics record now carries an `observedAt` failure timestamp; recovery is derived from the task's completion edit. Sentinel discipline throughout — a rate with no denominator and a duration with no completed-with-timing samples return `null`, never a fabricated `0`, so "no data yet" can't read as "0% success". (Further #2328 slice remains: full 4-tier fallback *execution* wiring — the classifier still annotates the investigation task rather than attempting each tier's fix in sequence.)
- **[issue-2333] Untyped task executions are now auto-classified instead of piling up as `unknown`.** Task-learning's `extractTaskType` used to dump every execution it couldn't pattern-match into a single blind `unknown` bucket — those tasks bypassed routing logic, skipped outcome learning, and leaked unstructured failures. It now runs an execution-entry classification hook (`classifyUntypedTask`, pure/idempotent/<50ms) that, before giving up, infers a concrete domain from additional signals — a `selfImprovementType` metadata hint, an explicit `task.taskType` the primary extractor didn't special-case (e.g. `scheduled` → `scheduled-task`), and broader whole-word description keyword signatures (`fix`/`investigate`/`crash` → `auto-fix`, `refactor`/`optimize` → `self-improve:general`, `review`/`audit` → `idle-review`, `unit test`/`coverage` → `test-task`) — and only falls back to a new sandboxed `external/untyped` type when nothing matches. Newly-classified and fallback executions flow through the existing #2329 failure-signature harvesting so their failures are captured in the normalized telemetry schema. The `external/untyped` bucket is explicitly walled off from routing (it never drives a model-tier suggestion and is never globally skipped) so a heterogeneous grab-bag can't create a routing blind spot or loop. (Partial delivery of #2333; the spawn-time key mirror in `agentModelSelection.extractTaskTypeKey` and deeper self-improvement-loop consumption of the harvested `failureSignatures` remain.)
- **[issue-2333] The spawn-time routing lookup key now mirrors the recorded domain, and the self-improvement loop reads the enriched failure signatures (the #2333 remainder).** Two follow-ons close out #2333. First, `agentModelSelection.extractTaskTypeKey` — the key used at spawn time to look up historical routing performance — no longer falls back to a blind `'unknown'`; it now delegates the fallback to the same `classifyUntypedTask` the completion path records under, so an untyped task's routing lookup keys off the exact bucket its outcome will be recorded in (a concrete inferred domain, or the sandboxed `external/untyped`) instead of a bucket that never matches. Second, a new pure `summarizeFailureSignatures` reader turns the per-completion `failureSignatures` map (provider/model-attributed, success-criteria-aware) into a ranked, display-ready summary that the self-improvement loop now consumes: `getPromptImprovementRecommendations` emits a provider-attributed "route this task type away from X" suggestion (escalated to high priority when recent runs missed a declared success criterion) and exposes the structured signatures, and the Learning insights view surfaces the summary plus a provider/model-named recommendation once a provider crosses three recent failures — attribution the coarse `errorPatterns` count could never express. This closes #2333.
- **[issue-2344] Task-learning now stamps a success-criteria validation verdict on every completion and gates routing aggressiveness on a correlation-quality window (the #2329 remainder).** Each completion now carries an explicit `validationPassed` boolean — "did the run meet its DECLARED success criteria?" — distinct from the runner's exit-code `success`, so a task-referencing-commit task that exits clean but committed nothing is honestly recorded as a miss. Computed once at the shared completion chokepoint (`finalizeAgent`) via the existing `[task-<id>]` commit check and threaded through `buildTaskTelemetryContext`, the persisted agent result, and the harvested failure-signature samples; a null sentinel means "no machine-checkable criterion was declared" (interactive/user tasks, user-terminated runs) so "not declared" never collapses into "declared and failed". Separately, a new rolling correlation-quality window (`server/services/taskLearning/correlationQuality.js`) pairs each run's failure-signal *prediction* (did the enriched signal flag this run's tier, from history before the run) with its actual outcome and scores their correlation via the Matthews/phi coefficient — with a null sentinel (not `0`) for a degenerate window where a class is absent. `suggestModelTier` gates auto-adjustment aggressiveness on that score: until it clears >0.8 with enough samples, tier-avoidance requires a higher failure-sample bar (5 vs 3) so the system doesn't over-correct on an unproven signal. The gauge is surfaced on the Learning insights payload. This closes out the two deferred #2329 slices.
- **[issue-2329] Model routing now consumes the enriched failure signatures, and the latency split derives a queue/compute breakdown when a source timestamp is missing.** The `failureSignatures` telemetry recorded per completion (provider/model/tier-attributed, recency-bounded) is now read by `suggestModelTier`: a new pure `deriveFailureSignalAvoidance` cross-references those recent failures against `routingAccuracy` and steers selection off a tier that is BOTH accumulating recent failures for a task type AND unproven (success rate < 80%, or no success data) — catching a freshly-degrading tier before its all-time rate crosses the hard misroute line, while a proven tier keeps its slot despite absolute failures (signatures record failures only, so counts alone can't condemn a tier). The dominant failing provider/model is surfaced as a `failureSignal` on the suggestion and named in the model-selection log, so the routing decision is legible rather than "avoid tier X". Separately, the queue/compute latency split now falls back to `wall − compute` (or `wall − queue`) when a run carries no `createdAt` or no runner duration, with a per-leg `source` marker (`measured`/`derived`/`null`) so a derived value is never confused with a measured one and a legitimate `0ms` is never confused with an absent leg. (Further #2329 work remains: success-criteria validation booleans on every completion path and the >0.8 correlation-quality measurement window.)
- **[issue-2329] Task-learning now records a structured telemetry context for every completed agent.** On each completion the learning system builds a `TaskTelemetryContext` — a failure signature (`{ category, messageSnippet, failurePosition }`, reusing the category `agentErrorAnalysis` already classified), execution context (`provider`, `model`, `modelTier`, `taskType`, `component`, input-size, and the routing decision rationale), and a latency breakdown (`wallMs` spawn→completion vs. `queueMs` wait vs. `executionMs` measured) — using explicit null sentinels for anything not cheaply derivable rather than fabricating values. Failure signatures are aggregated per error category into a new additive `failureSignatures` block in `learning.json` (older files without the key load unchanged), and the enriched context is surfaced on the single-line completion log. (Partial delivery of #2329; routing auto-adjustment that consumes these signals, success-criteria validation flags, the queue/compute split where spawn timestamps are missing, and correlation-quality measurement remain.)

## Fixed

- **A DevSecOps hardening pass makes background services fail safely and persistent controls touch-accessible.** Autofixer process/history data now renders through safe DOM text nodes and its periodic scan cannot overlap itself; CLI runner terminal events finalize exactly once; port discovery fails closed with deterministic cross-platform parsing; backup `rsync` lookup is portable; task-watcher events, Spotify requests, and shared media streams now have bounded/owned lifecycles; repeated CyberCity district-bound calculations use one empty-safe helper; and persistent mobile navigation plus timeline removal controls provide 44×44 touch targets with accessible labels. Focused regression coverage accompanies each repaired path.

- **The client test suite no longer floods CI logs with ~100 React `act(...)` warnings.** Twelve test files rendered components whose mount effects fetch data through pre-resolved mock promises and never settled them, so the `.then(setState)` microtasks landed outside `act` — invisible locally (vitest hides console output from passing tests) but printed in full by the CI reporter, burying real failures, and marking exactly the unsettled-async pattern behind our known CI test flakes. Each affected file now drains those updates inside act via a small settle-after-render helper (`await act(async () => {})`), with no assertion changes; tests that deliberately assert an in-flight pending state settle at the end instead. Promoting this to an enforced zero-warning gate in `src/test/setup.js` is tracked in #2406.

- **CoS tasks with Review Loop turned off now explicitly disable saved external-review defaults when opening a PR.** Their completion prompt passes `--review-with none`, so an inherited Copilot (or other reviewer) default cannot launch a review that was not requested.

- **[issue-2387] Theme initialization and switching now survive unavailable localStorage.** The `useTheme` and `useCitySettings` hooks read/wrote `localStorage` directly (persisted theme, and the CyberCity settings that render on the same theme path), so a browser where storage access throws (Safari private mode, blocked cookies, disabled storage) or is absent would throw inside a `useState` initializer and crash the initial render, and a failing `setItem` could break theme switching. Both hooks now route every read/write/remove through a shared, guarded `safeReadStorage`/`safeWriteStorage`/`safeRemoveStorage` helper (`client/src/lib/safeStorage.js`) that swallows access failures, and corrupt stored JSON falls back to defaults instead of throwing — so init always resolves to a valid default and in-memory switching keeps working when persistence fails. `setTheme` also applies the in-memory theme (DOM + state) before any persistence/side-effect work so a later failure can never leave the UI on the stale theme. Focused hook tests cover read-throws-on-init, corrupt/invalid stored value, and write-throws-on-switch for both hooks.

- **[issue-2384] A usage-limit fallback signal now stops a direct-CLI CoS agent immediately even when an earlier transcript write is stalled, and the persisted transcript can no longer be reordered.** The direct-CLI spawn path (`spawnDirectly` in `server/services/agentCliSpawning.js`) let each stdout/stderr `data` event run its transcript-mutating body (`outputBuffer` append + `output.txt` write + batched live-tail push) as an independent async handler, so two chunks' interleaved `await`s could persist output out of order, and a blocked state/file write on an earlier chunk could sit in front of a later chunk that carried the `Now using extra usage` fallback marker. Fallback detection now runs **synchronously** in the raw listener — before any transcript work is enqueued — so the provider is killed the instant the signal arrives regardless of a stalled write, while the transcript body is serialized onto a single per-agent tail promise (mirroring `shell.js`'s `hookQueue` and `pipeline/issues.js`'s `issueWriteTail`) that the close/error/terminate paths drain before finalizing. A regression test blocks an earlier write, emits the fallback signal on a later chunk, and asserts termination stays immediate and final output stays ordered.

- **CoS agents (notably codex) now open their PR as the gh account that matches the repo owner, not gh's mutable active user.** PortOS's own `createPR` pins `GH_TOKEN` to the logged-in gh account whose login matches the repo owner (avoiding the multi-account "must be a collaborator" failure), but a spawned agent running its *own* `gh pr create` — a codex/Claude/CLI CoS agent finishing `/do:pr` — never received that pinning. TUI agents fared worst: `shell.js`'s `buildSafeEnv` strips `GH_TOKEN` from the inherited env entirely, so their `gh` fell back to whatever `hosts.yml` active user happened to be set. A new best-effort `resolveForgeTokenEnv(dir)` (in `server/services/git.js`) resolves just the owner-matched `GH_TOKEN` overlay and is injected into all three agent-spawn env paths — direct CLI (`spawnDirectly`), TUI shell session (`spawnTuiAgent`), and the CoS runner (`spawnViaRunner`). It's provider-agnostic (no codex special-case), returns `{}` when no owner-matched account exists (so single-account installs and GitLab repos are untouched), and is spread before `provider.envVars` so an explicit provider `GH_TOKEN` override still wins.

- **Codex CoS agents no longer die `paste-not-rendered` while codex boots its MCP servers.** Codex boots any MCP servers configured in the user's global `~/.codex/config.toml` on every headless spawn — including heavyweight interactive ones like `playwright` (fetched via `npx`) or a `node_repl` with `startup_timeout_sec = 120`. That boot can run for tens of seconds up to ~2 minutes, during which codex swallows pastes, renders no `[Pasted Content N chars]` commit marker, and shows only the *tail* of a long paste in its input viewport (never the verified prefix) — so PortOS's paste-verify retry could not confirm delivery and killed the agent after only 3 short attempts (~19s), long before the boot finished (real incident 2026-07-10: `agent-c5a26b40` / `agent-3f4ae3b1`, both on gpt-5.6-sol; the same prompt succeeded days earlier when no MCP servers were configured and codex was ready in <10s). The spawner now detects codex's MCP-boot banner (`Booting MCP server` / `Starting MCP servers`) and extends the paste-retry budget from the fixed 3 attempts to a time budget (150s, covering `node_repl`'s 120s startup plus an `npx` cold download while staying under the idle-reap window), so a slow boot completes and the paste finally lands. If the boot genuinely never finishes, the failure now names the MCP-boot cause and points at the codex config instead of a misleading "still initializing" message. Non-codex TUIs and codex installs without MCP servers are unchanged.

- **Read-only CoS agents (e.g. reference-watch) on a TUI provider now signal completion via the `.agent-done` sentinel.** The prompt builder's completion decision tree gave every other task shape (reasoning-only, TUI push+PR, CLI) an explicit sentinel instruction, but the `isReadOnly` branch emitted only the bare "Read-Only Task" notice with no sentinel — so a read-only TUI run (reference-watch records its proposals directly in the tracker, then just needs to signal done) never wrote `.agent-done`. The 2s sentinel poll in `spawnTuiAgent` — the primary finalize path and the channel that ingests the run summary — therefore never fired, and the run only completed via the idle-reaper / shell-exit fallback with no clean resolution summary captured. `agentPromptBuilder.js` now routes the read-only branch through a new `buildReadOnlyCompletionSection({ isTui, sentinelPath })` that keeps the do-not-modify guidance and, for TUI agents, appends the write-`.agent-done`-then-stop instruction. CLI/API read-only agents complete on process exit and never poll a sentinel, so they still get the bare notice.

- **Digital Twin Taste questionnaire no longer shows "Question 5 of 3" after follow-ups.** The progress header used total response count (core + follow-ups) as the current question number against a core-only total, so once branching follow-ups fired the counter overran the "of N". It now reports the 1-based index among core questions for that section, so the label stays "Question 2 of 3" even when follow-ups sit between cores.

- **Schedule Timeline now explains why a task sits on "Now" instead of looking like a bug.** A weekly task that has never run, or a cron task whose scheduled slot passed while it was down, is genuinely *due now* — the scheduler will fire it on the next check as a first-run or catch-up. The timeline drew that NOW launch identically to an on-cadence launch, so a marker on "Now" next to a "Sun at 07:00" / "weekly" label read as wrong (e.g. `branch-cleanup` and `reference-watch` both showing NOW). Due-now launches are now tagged server-side (`dueNow` + the scheduler's reason and any missed cron slot) and rendered distinctly: an amber marker at Now (kept on a separate visual channel from the collision warning ring so a launch that is both due-now and colliding shows both states), a `first run` / `catch-up` / `overdue` badge on the Scheduled-order chip, and tooltips/legend explaining that it launches on the next check rather than waiting for the next cadence slot. On-cadence future launches are unchanged.

- **Accessibility: icon-only controls, keyboard-trapped elements, and unlabeled form fields across the app are now screen-reader and keyboard accessible.** An audit found ~65 icon-only buttons (modal/panel close `✕`, calendar prev/next, refresh, inline-edit save/cancel/edit/delete, reorder, delete, skip/back) with no accessible name, 15 `<div>`/`<span>` elements that were clickable by mouse only (no keyboard focus or Enter/Space activation), and several settings/config inputs missing a `<label>` association. Buttons gained descriptive `aria-label`s; clickable non-buttons now use a new shared helper `clickableProps()` (`client/src/lib/a11yKeyboard.js` — adds `role="button"`, `tabIndex`, and Enter/Space `onKeyDown`) so keyboard users can reach and activate them; and the form fields (external SD API URL, Python interpreter path, test-render prompt, per-row AI provider/model selects, image-gen seed) got proper `htmlFor`/`id` or `aria-label` associations. `IconPicker` also gained Escape-to-dismiss and an `aria-hidden` backdrop. The shared shells (Layout, Modal, Drawer, ⌘K, Logo, MediaImage) were already accessible and unchanged.

- **Sidebar nav no longer truncates long link names.** App names (and other child labels) under Apps and the rest of the sidebar used `truncate` in a 224px rail, so names like `barnhub.online` or `IdeatorPro…` collapsed to ellipsis with no way to read them. Labels now wrap with `break-words`, always expose the full name via `title`, the pin control is overlaid so it no longer steals label width, and the expanded sidebar is slightly wider (`w-64`).

- **[issue-2337] Path-configured CLI/TUI providers now resolve their real vendor context window.** `knownProviderContextWindow` (mirrored in `server/lib/stageRunner.js` and `client/src/utils/providers.js`) matched the provider `command` with exact equality, so a Codex/Antigravity/Grok provider configured with an absolute or relative command path (e.g. `/opt/homebrew/bin/grok`) was missed and fell back to the generic 128K CLI default instead of the vendor window (Codex 1M, Antigravity 1.05M, Grok 256K). Both mirrors now normalize the command to its basename before comparing — the server reuses `commandBasename` from `providerModels.js` and the client uses an inline lockstep helper — matching how the arg-builder predicates already route. Low impact in practice (shipped providers carry an explicit `contextWindow`; only hand-configured path-based providers without one were affected).

- **[issue-2328] Auto-fixer now has a circuit breaker so a persistently-failing resource can't flood the task queue.** The auto-fixer only deduped identical errors within a 60-second window, so a provider/model (or critical error) that failed every ~minute would queue a fresh investigation/fix task indefinitely — a runaway loop that exhausts the plan queue and never gets healed by yet another identical task. It now trips a per-resource circuit after more than 3 task-worthy failures within a rolling 1-hour window, suppressing further task creation (logged as `🔌 Auto-fix circuit OPEN …`) until the failure rate ages out and the circuit auto-closes. (Partial delivery of #2328's guardrails; the tiered fallback chain, structured failure-reason telemetry, and dashboard metrics remain.)
- **[issue-2328] Auto-fixer now classifies every failure into a fallback tier and records structured per-attempt diagnostics.** A deterministic classifier maps each error category (config/env, schema/type, transient, or unknown) to one of four fallback tiers — 1 config/env, 2 schema/type, 3 constrained-agent-retry, 4 escalate — with unrecognized categories always escalating so nothing is silently swallowed. Each auto-fix attempt now builds a structured `{ triggerEvent, target, errorType, category, tier, fixStrategy, failureReason }` diagnostics object that rides on the investigation/fix task record (and its agent-facing context) and is surfaced inline in the existing single-line log (`… tier=1 (config/env) …`), so failures can be broken out by tier / category / reason. (Further #2328 slices remain: dashboard telemetry surfacing, time-to-recovery metric, full 4-tier execution wiring.)
- **[issue-2328] Auto-fix diagnostics are now persisted as structured task metadata instead of being silently dropped.** The tier/category/failure-reason diagnostics the classifier builds for every error-driven task were only ever embedded into the free-text context string and the log line — `addTask` never copied the structured object into task metadata, so it couldn't be queried or aggregated afterward. It now persists `taskData.diagnostics` as first-class `metadata.diagnostics` (round-tripping through the markdown store via the existing JSON sentinel, with a defensive non-object guard), and `createCosTaskSchema` validates it for schema parity. This is the prerequisite for the remaining #2328 telemetry slice: aggregating auto-fix outcomes by tier / failure reason / time-to-recovery and surfacing them on the dashboard.
- **[issue-2342] Auto-fix now actually *attempts* the cheapest fallback tier before escalating, instead of only annotating the tier.** The tier classifier (#2331) labeled each provider failure with a fallback tier but the runner only ever did one thing on failure — swap to a fallback provider (Tier 3) — and otherwise queued a Tier-4 investigation task. `runPromptThroughProvider` now walks the tiers in order: a `model-not-found`/`model-not-supported` failure first gets a deterministic **Tier 1 (config/env) correction** — a bounded retry of the *same* provider with a valid model it lists (no persisted config change; skips embedding-only ids) — and only if that declines/fails does it fall through to the **Tier 3** fallback-provider retry, then the **Tier 4** investigation task. Recovery at any tier is reconciled through the existing `noteFallbackStarted`/`noteFallbackHandled`/`noteFallbackFailed` lifecycle, so a Tier-1 recovery suppresses the investigation task exactly like a Tier-3 one — a fully-recovered failure leaves **zero** tasks, and only genuinely unrecovered failures escalate. The recovering tier is surfaced on the result (`fixTier`/`fixStrategy`) and in the single-line log (`🔧 Tier 1 (config/env) retry …`), and because fewer failures reach a Tier-4 task the existing #2328 telemetry automatically reflects the per-tier recovery. (Tier 2 schema/type has no safe deterministic same-provider transform at the runner layer — a malformed request/response can't be rewritten without the caller's schema — so it falls straight through to the bounded retry.)
- **[issue-2350] Tier 2 (schema/type) now performs a real deterministic same-provider request/response correction instead of falling straight through.** Carved out of #2342: at the runner layer a schema/type failure never arrives as a provider *error* (the provider returns HTTP 200), so a malformed response can only be recognized against the caller's declared shape. `runPromptThroughProvider` gained an optional `responseSchema` (a Zod-style schema or a bare predicate) — and an optional `repair` normalizer callback — so a caller can now declare the response it expects. When the response fails that schema the runner attempts a **bounded deterministic JSON coercion in place** (strips markdown fences / prose and pulls the first schema-matching block, reusing `jsonExtract.extractJson`); if the shape still can't be recovered it **re-requests the same provider once with a schema-strengthened prompt** before falling through to the Tier-3 fallback provider and the Tier-4 investigation task. Every recovery is reconciled through the existing `noteFallbackStarted`/`noteFallbackHandled`/`noteFallbackFailed` lifecycle exactly like Tiers 1 and 3, so a Tier-2 recovery leaves **zero** investigation tasks; the recovering path is surfaced on the result (`fixTier: 2`/`fixStrategy: 'schema/type'`, plus `coercedResponse: true` for the pure in-place coercion) and in the single-line log (`🧩 Tier 2 (schema/type) retry …`). A schema/type failure never benches the healthy provider (it's response-specific, not an outage). The Messages triage caller declares its expected JSON-array shape as the first adopter.
- **Layered Intelligence loop now supports every provider type.** The per-app AI-provider picker previously listed only `cli` providers, while the reasoning call went through the api-only `callProviderAISimple` — so a selected CLI provider failed at runtime with "requires an API-based provider," and API/TUI providers (Ollama, LM Studio, Claude/Codex/OpenCode TUI, etc.) never appeared at all. The reasoning call now routes through the unified `runPromptThroughProvider`, which dispatches on `provider.type`, and the picker lists all enabled `cli`/`api`/`tui` providers. CLI/TUI spawns run in the app's repo (`cwd`).
- **A "Run now" of Layered Intelligence now tells you what actually happened.** Because the loop is handler-backed — it files one tracker issue rather than spawning a visible agent — a run that filed nothing gave no usable signal: the on-demand toast collapsed every non-filed outcome (reasoning model returned unparseable JSON, a duplicate/near-duplicate was suppressed, it parked on a blocking issue, no AI provider was configured, Jira wasn't set up, the tracker read failed) into a blanket "nothing to do right now," and on mobile that transient toast was easy to miss entirely. The handler now threads the real terminal reason through the on-demand feedback (so the toast reads e.g. "the reasoning model returned no usable JSON — try a non-reasoning model" or "paused on a blocking issue"), and every run persists its outcome (`lastRunAt` / action / reason / filed ref) so Edit App → Intelligence shows a durable **Last run** status line you can check any time. This is what made a reasoning-style local model (which often emits thinking text with no parseable JSON) look like a queued-but-not-running task with no explanation.
- **Rounds and Music Video can now turn a reference audio file into MIDI locally via MuScriptor.** MuScriptor (github.com/muscriptor/muscriptor) is a multi-instrument audio → MIDI transcription model that runs in an opt-in venv (`INSTALL_MUSCRIPTOR=1 bash scripts/setup-image-video.sh`, weights auto-download from HuggingFace on first use; small/medium/large tiers). A new shared service (`server/services/audioMidiTranscription.js` + the `scripts/transcribe_muscriptor.py` sidecar, same STAGE:/RESULT: wire protocol as the music-gen sidecars) exposes the kickoff/SSE/cancel job shape in both features: a Rounds reference with attached audio gains a **Transcribe MIDI** control (the resulting `.mid` lands as `references[].midiFilename` on Save, cleared with the audio like segments), and a Music Video project gains a **MIDI** toolbar button + download link (`project.midiTranscription` is persisted server-side on completion and invalidated when the audio source changes, exactly like `audioAnalysis`). When the venv isn't provisioned the kickoff 503s with the install hint instead of a bare spawn error.

- **[issue-2386] ffmpeg render errors are now separated into pre-spawn and post-spawn, so a failed kill can no longer let a replacement render overlap the live process.** The music-video and timeline renderers both routed every `'error'` event through the same immediate finalizer — which released the per-project render mutex and dropped process ownership. But `'error'` fires for two very different cases: a genuine pre-spawn failure (ENOENT — the child never started, no `'close'` follows) and a *later* child-process error such as a failed kill while ffmpeg is still running. Releasing the mutex on the latter let a re-render spawn and overlap the still-live ffmpeg. Both services now track spawn state (a `'spawn'` listener sets a flag) and finalize immediately only for pre-spawn errors; a post-spawn error is recorded but retains process + project-mutex ownership until the pending `'close'` runs the sole terminal finalization. A shared `terminal` guard makes terminal handling exactly-once across the error and close paths, and both handlers wrap their body per the child-process-callback convention. Covered by new error-to-close sequence tests before and after spawn in both services.
- **[issue-2375] Split the ~1,380-line `server/routes/apps.js` into domain-focused sub-routers.** The single apps router (24+ endpoints spanning CRUD/status enrichment, PM2 lifecycle, config repair, Xcode provisioning, icon serving, task-type overrides, document git-commits, editor/folder launch, and agent history) is now assembled from nine mountable sub-routers under `server/routes/apps/` (`crud`, `lifecycle`, `viteTls`, `xcode`, `icons`, `taskTypes`, `launch`, `documents`, `agents`), mirroring the pipeline sub-router pattern. Shared plumbing (`loadApp` middleware, `pathExists`, and the port/overall-status derivation helpers previously duplicated between the list and detail endpoints) lives in one `apps/shared.js`, and the ~130-line ecosystem-config port-rewrite orchestration moved out of the PUT handler into a `server/services/appPortConfig.js` service. The `/api/apps` route contract is byte-for-byte identical (all 35 path+method pairs verified) and the oversized route test file is split along the same domain boundaries. No behavior change.
