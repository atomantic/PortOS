# Unreleased Changes

## CoS Agents

- **An explicit "Run" of a perpetual claim/reconcile task that finds no work now tells you so — instead of silently doing nothing.** Triggering an on-demand `claim-issue` (or any perpetual task) that had nothing actionable produced zero UI feedback: the task parked with a log line buried in pm2, so the user was left wondering why "40 open issues" didn't spawn an agent. The on-demand path now (1) resets any existing park **and the branch/issue-reconcile convergence signature** before re-running the detector, so a manual trigger always re-checks live state and never honors a stale/cached verdict — previously an explicit "Run" of `branch-reconcile`/`issue-reconcile` could re-park `no-progress` against an unchanged-since-last-run set even though the user asked to re-drive; and (2) when the fresh check still finds no work, emits a `cos:schedule:on-demand-empty` event that surfaces a toast — e.g. *"claim-issue: re-checked now — no claimable issues (0 of 40 claimable — 2 in-flight, 38 filtered). Next auto-recheck in 33 hours."* The GitHub/GitLab work-detector now returns a breakdown (`open`, `inFlight` held by open claim branches/PRs, `filtered` skipped by label/assignee/epic), plumbed through the park record and event, so the message explains WHY a non-empty queue yields zero claimable work — the common case being issues already shipped whose stale `claim/issue-N` branches still count as in-flight. The event carries an `outcome` so the message fits the case: `parked` (perpetual detector found no work), `transient` (a `gh`/`glab` probe failure — *"couldn't complete the check just now — try again"*, never a false "nothing to do"), or `idle` (a non-perpetual task like `pr-watcher` genuinely had nothing — a calm neutral note, not an error). Both on-demand drain engines — `cos.dequeueNextTask` and `cosTaskGenerator.spawnPriority0OnDemand` — share the reset-and-emit path (via `emitOnDemandEmpty`), so a user Run gets feedback no matter which engine picks it up. The event fires ONLY for user-initiated on-demand runs (never background parks), so there's no toast spam. (`server/services/cos.js`, `server/services/cosTaskGenerator.js`, `server/services/perpetualWork.js`, `server/services/taskSchedule.js`, `server/services/socket.js`, `client/src/hooks/useOnDemandTaskToast.js`)

- **The proactive voice agent now announces a task's topic, not the raw prompt block.** When a voice-dispatched CoS task became ready or finished, the spoken line read the first ~240 characters of the task's `description` — which for a scheduled swarm/claim run is the `# ⚡ SWARM MODE — claim and ship up to 3 independent issues in parallel. This run operates in slashdo /do:next --swarm=3 mode. …` prompt scaffolding, so the assistant recited the orchestration preamble instead of naming the work. Both the "new task is ready" and "task is done" lines now derive a short topical label via a shared `deriveTaskSpeechLabel` helper that prefers an explicit title or the post-run agent summary and otherwise strips the first meaningful description line down to its topic (leading markdown heading, emoji, `[Claim Issue: App]` tag, `**bold**` markers, and the `SWARM MODE —` label), clipped to one phrase — so the swarm task speaks "claim and ship up to 3 independent issues in parallel" and a plain claim task speaks "Claim and ship the next open GitHub issue". (`server/services/voice/proactiveTriggers.js`)

- **[issue-2259] `issue-reconcile` now heals JIRA zombie tickets as well as GitHub/GitLab issues.** Following the GitLab sibling (#2249), the zombie-scan is now JIRA-aware. JIRA has no `in-progress` label to release, so the zombie is STATUS-based: a ticket left **In Review** (the claim-issue-jira success end-state) whose MR/PR shipped with remaining scope and NO live `claim/<KEY>` branch anywhere. Detection flows through the PortOS JIRA API (`fetchMyCurrentSprintTickets` — a new non-swallowing variant of `getMyCurrentSprintTickets` so a transient API blip skips-without-parking instead of being misread as an empty sprint) + git-ref scan for `claim/<KEY>` / `cos/…/<KEY>/…` claim branches — no `gh`/`glab` equivalent. JIRA tickets normalize into the SAME common `{ number, title, url, status, hasMergedPr, hasLiveClaim, hasActiveAgent }` shape the forges use, so the pure classifier/signature stay shared: In-Review → ZOMBIE, plain In-Progress → STALLED, To Do / Done → excluded so the scan converges. Routing mirrors `resolveAppWorkTracker` — JIRA is NEVER auto-selected from the git host; it's chosen only when the app's resolved `workTracker` is `'jira'` with enabled `jira.instanceId`/`projectKey`. The coordinator prompt gains a JIRA arm (heal via ticket transitions + `POST /api/jira/instances/:id/tickets` rather than close+relabel), bumped to v3 with the v2 body preserved in `PREVIOUS_DEFAULT_PROMPTS`. (`server/services/issueReconcile.js`, `server/services/jira.js`, `server/services/cosTaskGenerator.js`, `server/services/taskPromptDefaults/`)

- **Claude Code TUI agents no longer fail `paste-not-rendered` on every multi-line prompt.** The #2192 paste-verification guard confirmed a paste had landed by looking for the prompt's literal text in the TUI buffer — but Claude Code collapses a multi-line bracketed paste into a `[Pasted text #N]` chip and *hides* the pasted body text, and it reflows single lines with cursor-positioning escapes instead of literal space bytes between words. Both made the text check false-negative, so every `claude-code-tui` CoS agent retried 3× and died with `paste-not-rendered` despite the paste landing perfectly (100% reproduction across real runs, 2026-07-05). The gate now treats the TUI's own paste-commit **marker** as authoritative proof of delivery (checked first, via a new `isPasteConfirmed` helper), and only falls back to text matching for the markerless path — where the match is now whitespace-insensitive so a space-less reflowed line still matches. Captured transcripts from the real incident are pinned as regression tests. (`server/lib/tuiHandshake.js`, `server/services/agentTuiSpawning.js`)

- **[issue-2228] A TUI-transcript-analysis prompt that itself embeds a `[Pasted text #N]` literal no longer false-fails the paste.** The marker-authoritative gate above subtracts the marker count the prompt itself carried, so an echoed `[Pasted text #N]` isn't mistaken for the TUI's own commit — correct for the inline (uncollapsed) path, but wrong when Claude Code *collapses* a multi-line self-marker-containing prompt into its own single chip and hides the body: the buffer then shows one marker while the prompt also carried one (`1 > 1` is false), and the hidden body also defeats the text fallback — so the agent died `paste-not-rendered` despite the paste landing. Paste confirmation now also recognizes Claude's collapsed-chip shape (a marker present alongside the "paste again to expand" affordance) as authoritative proof of delivery — chrome the TUI only renders on its own genuine commit, so it can't re-introduce the echoed-marker false-positive the subtraction guards. A misleading code comment claiming the collapsed transcript paste "simply falls back to the timer, which is safe" is corrected. (`server/lib/tuiHandshake.js`)
- **A live agent TUI no longer gets reaped mid-paste.** The idle-timeout reaper only tracked `lastOutputAt` (PTY output), not client input, so pasting a large multi-line command into a running agent's session could sit in a silent reflow/commit window with no echo yet — indistinguishable from "idle" to the 5s poll. If the session happened to already be near its idle threshold, the reaper killed the PTY mid-paste, showing "[Session removed externally]" in the Shell page. The idle timer in `agentTuiSpawning.js` now also checks a new `shell.js` export, `getLastInputAt()`, and skips the reap while input arrived within the last 15s. This is deliberately keyed on input *recency*, not "is a Shell viewer attached": a regular (non-external) session's socket stays bound after its viewer navigates away — only external one-shot `tuiPromptRunner.js` runs get released via `shell:release-views` — so an attachment-based guard would have permanently suppressed idle-complete for any agent glanced at once in the Shell UI. (`server/services/shell.js`, `server/services/agentTuiSpawning.js`)
- **Swarm claim-issue runs now write the completion sentinel so the task actually finishes.** In `/do:next --swarm` mode the orchestrator ships every issue via a PR and never touches the working tree, so it was skipping the `.agent-done` completion sentinel — leaving the CoS task hanging with no summary as if the run never ended. Phase C of the swarm directive now explicitly instructs the orchestrator to write the sentinel (with a run summary of the issues claimed, their PRs, and merge outcomes) after the serialized merges, while still skipping the per-agent `/simplify`/push steps. (`server/services/cosTaskGenerator.js`)
- **[issue-2190] OpenCode Ollama providers now accept any --model flag.** The static `OPENCODE_CONFIG_CONTENT` lacked a models map, so OpenCode rejected every model with "not valid" and the agent sat idle. The config is now built dynamically at spawn time, declaring the provider's configured models (plus the model being run) under `provider.ollama.models` with bare ids — the exact shape OpenCode's `@ai-sdk/openai-compatible` provider validates `--model ollama/<id>` against. Applied across every OpenCode spawn path (CoS CLI/TUI agents, the toolkit CLI/TUI runners, and the light fire-and-collect CLI run), and a migration backfills existing installs' `data/providers.json` so a configured model's map is persisted too. Fixes the TUI eval path for local Ollama models.
- **[issue-2243] OpenCode Ollama (and every other CLI provider) now starts — and stops cleanly — under Chief of Staff on Windows.** A CoS task using OpenCode Ollama — or any npm-installed CLI provider — failed instantly with a `startup-failure` (exit code -4058) while the same provider ran fine from the Settings "Run Prompt" button. The CoS agent spawners launched the bare command (`opencode`) directly, so on Windows the `opencode.cmd` shim couldn't be found and the process never started; the "Run Prompt" path already resolved the real shim and launched it through `cmd.exe`. Both CoS spawn paths (the runner process and direct mode) now use that same resolve-and-wrap step, so the agent starts correctly. The runner path additionally now injects the OpenCode models config (the #2190 fix), so the model is accepted there too. And because a launched agent is now a `cmd.exe`-wrapped process on Windows, stopping, pausing, or terminating it routes through a process-tree kill so the real CLI child is taken down too instead of being orphaned. Non-Windows installs are unaffected.
- **A busy-but-stuck TUI agent can no longer run unbounded — a wall-clock ceiling now backstops the idle reaper.** The idle-timeout reaper resets on every PTY chunk, but Claude Code repaints its `(Ns · esc to interrupt)` working counter roughly once a second while *any* tool/API call is in flight — including one stuck retrying a stalled Bedrock/network operation. A "busy-but-stuck" agent therefore keeps `lastOutputAt` advancing forever, so idle-reap never fires and the run had no ceiling at all: a real `/do:next --swarm` claim-issue orchestration churned its counter for 98 minutes (2026-07-06, agent-b1c56083) before Claude Code's *own* internal "Operation timed out" finally stopped it — had the CLI not self-terminated, the agent would have run unbounded, holding its lane, blocking the app's cooldown, and leaking a shell session. The long-running agent TUI path now arms an absolute wall-clock backstop from prompt submission (default 3h, provider-configurable via `tuiMaxRuntimeMs`), mirroring the one-shot runner's existing `hardTimeoutTimer`. If it fires with no `.agent-done` sentinel present, the run finalizes as a needs-manual-finish FAILURE (`max-runtime-timeout`) with the same "check for open or merged-but-uncleaned PRs" guidance as the merge-queue/review-loop idle-timeout paths, capturing any uncommitted worktree diff for post-mortem first; if the sentinel *is* present the run is salvaged as success. (`server/services/agentTuiSpawning.js`, `server/lib/tuiHandshake.js`, `server/lib/aiToolkit/providers.js`, `server/lib/aiToolkit/validation.js`, `server/lib/validation.js`)
- **[issue-2191] TUI agents with zero file changes are now marked failed instead of silently succeeding.** A TUI agent that idled out after showing working indicators but made no uncommitted file changes was previously finalized as `success: idle-complete` — deleting the worktree and any evidence of what happened. Now the idle-complete path checks `git status` in the worktree: if the tree is clean, it finalizes as `failure: idle-no-changes` with the TUI buffer tail as error context. Any uncommitted diff is also captured to `worktree-diff.txt` in the agent archive dir before cleanup, so post-mortems can see what (if anything) was left behind.
- **[issue-2193] Concurrent agent spawns no longer race on git worktree creation.** When two tasks for the same app spawned in the same evaluation tick, the first `git worktree add` grabbed git's per-repo worktree lock and the second failed with a lock error — blocking that task with `blockedCategory: worktree-failed` and leaving an orphan branch (`cos/task-…/agent-…`) with no worktree. Worktree creation now serializes per source repo through a single promise tail (the `issueWriteTail` pattern), so same-tick adds queue instead of colliding; `git worktree add` also retries a few times with backoff on lock errors to absorb residual external contention; and on final failure the partially-created branch is deleted (`git branch -D` + `git worktree prune`) so orphan branches stop accumulating. (`server/services/worktreeManager.js`)
- **Scheduled improvement tasks with no configured model now follow the active provider's default instead of a stale hardcoded one.** When a self-improvement or managed-app improvement task had no explicit model in its schedule config, the generator pinned a hardcoded `claude-opus-4-5-20251101` into the task — a model that had since dropped out of the `claude-code-tui` provider config, so the scheduler spawned `claude --model claude-opus-4-5-20251101` even after the provider default moved to Opus 4.8. Since a per-task model pin is honored above the provider default (it's passed straight through to the CLI), that stale literal overrode the configured default on every scheduled run. The generator no longer pins a literal: with no configured model it leaves the task's model unset, so model selection resolves the active provider's tier/default model at spawn time and always uses a model the provider actually lists. An explicit per-task or per-schedule model pin still wins as before. (`server/services/cosTaskGenerator.js`)
- **An explicit per-task model pin is now always honored — no more silent, expensive downgrades.** When you set a specific model on a CoS task, PortOS validated it against the provider's `models` list and, on any mismatch, *silently fell back to the provider's default (heaviest) model*. Because `claude-code-tui` lists the dated id `claude-haiku-4-5-20251001` while `claude-code` lists the undated `claude-haiku-4-5`, picking "haiku" on the TUI provider quietly ran **Opus** instead. Explicit user-specified models are now passed through to the CLI even when they're not in the convenience list (the CLIs accept any valid id; a genuine typo surfaces as a provider error rather than a costly silent substitution). Auto-selected models still fall back to the tier default as before.
- **Worktree tasks now default to opening a PR instead of auto-merging to your default branch.** A CoS task run in an isolated worktree with "Open PR" off merges its commits straight back to the main development branch on completion — fine for trusted agents, dangerous for unreviewed local-model work. Creating a worktree task now defaults "Open PR" on (in the UI, enabling Worktree checks it; via the API, a worktree user-task without an explicit `openPR` defaults to `true`). You can still turn it off per-task for a quick auto-merge, and internal automation (autopilot, self-improvement) keeps its existing auto-merge behavior so nothing starts silently waiting on a human to merge a PR.

- **Local-model Claude agents now launch in a lean, isolated mode.** CoS agents on the Ollama-backed Claude providers (`claude-ollama`, `claude-ollama-tui`) previously inherited your full personal Claude Code environment — hooks, plugins, MCP servers, global CLAUDE.md — which reliably derailed small local models (a live eval showed qwen3.6:35b answering a session-start hook instead of its task). These sessions now spawn with `--bare --strict-mcp-config` (no hooks, no plugins, no MCP tool schemas, no CLAUDE.md auto-discovery), authenticating through the provider's existing `ANTHROPIC_BASE_URL` + `ANTHROPIC_AUTH_TOKEN` (pointed at your local Ollama), and get the same plain-`git`/`gh` completion workflow OpenCode agents use instead of slashdo commands bare mode can't load. In the same eval, once isolated this way qwen3.6:35b wrote a correct, test-passing helper (verified 46/46) end to end — the failures were the environment, not the model.
- **Every Claude Code agent now separates its task from the PortOS operating contract.** For all Claude providers (TUI and headless CLI, local and hosted), the PortOS coordination boilerplate — worktree branch, completion-sentinel protocol, git/PR workflow — now rides in a real system prompt via `--append-system-prompt-file` (saved beside the agent's `prompt.txt` as `system-prompt.md`), while the pasted/stdin prompt carries only the task. The model weights the contract as standing instructions rather than conversation, and hosted providers get better prompt-cache reuse on the stable system block. The prompt content is byte-identical to before — only its placement changed. Codex, Antigravity, and OpenCode agents (no equivalent flag) keep the combined prompt unchanged.

- **Partially-completed issues no longer become un-claimable "zombies," and a new scheduled task heals any that already exist.** When a claim run shipped only PART of an issue's scope and merged the PR, the issue was left OPEN with its `in-progress` label (and assignee) still on — and since the claim queue treats `in-progress` as "claimed and being worked," the remaining scope was never re-picked and never finished (real cases: #2220, #2179, #2175). The completion flow was binary (fully-done → close + unlabel) with no "partial ship" path, and the branch reconciler is branch-centric so it never touched the linked issue. Two fixes: **(1)** the `claim-issue` / `-gitlab` / `-jira` prompts now choose the PR trailer deliberately (`Closes` for a full ship, `Refs` + a `## Remaining` section for a partial one) and Phase 7 reconciles the issue with an agent-judged hybrid — close + file a scoped follow-up when the remainder is a clean separable chunk, else post a `Done ✓ / Remaining ▢` comment and release the `in-progress` claim so the queue re-picks it (JIRA records remaining scope + files a follow-up ticket). **(2)** a new opt-in per-app `issue-reconcile` CoS task (sibling to `branch-reconcile`, disabled by default) runs a deterministic `gh`/`git` scan for zombie issues — open + `in-progress` yet with their PR merged and no live claim anywhere (no open PR, no local/remote/CoS claim branch, no active agent) — and dispatches a coordinator agent that applies the same hybrid per issue, with an `autoClose` toggle (OFF ⇒ comment + release only) and follow-up dedup so two federated peers can't file duplicates. Perpetual drain with a convergence signature, exactly like branch-reconcile. (`server/services/issueReconcile.js`, `server/services/cosTaskGenerator.js`, `server/services/taskSchedule.js`, `server/lib/cosValidation.js`, `server/services/taskPromptDefaults/`)

## Layered Intelligence

- **[issue-2262] A dedicated Layered Intelligence page now surfaces the self-improvement loop's status across every app.** The perpetual per-app loop was previously visible only inside each app's Edit-App → Intelligence drawer tab, so there was no single place to see which apps had it enabled, when each last ran / is next due, or whether the global sweep job is even on. The new `/layered-intelligence` route (Chief of Staff → Layered Intelligence, `⌘K`/voice-navigable) reads a deterministic cross-app overview — each active app's effective config plus scheduler bookkeeping (`lastRunAt`/`nextDueAt`/due) and the global job's on/off state — with no LLM or forge I/O. It warns when the global job is disabled (per-app configs are inert until it's on), lists enabled apps with their interval, telemetry sources, allowed scopes, and provider, and deep-links each app's "Configure" action straight into its Intelligence tab (`/apps/:id?edit=1&appTab=intelligence`, via a new auto-open trigger on the app-detail drawer). (`server/routes/apps.js`, `server/services/layeredIntelligence.js` `summarizeLoopStatus`, `client/src/pages/LayeredIntelligence.jsx`, `client/src/components/apps/AppDetailView.jsx`, `server/lib/navManifest.js`)
- **[issue-2262] The loop now catches near-duplicate proposals worded differently, not just exact-slug repeats.** The DECIDE layer's dedup was purely the reasoner-chosen slug (plus label) — so if the model proposed the same work under a fresh slug ("introduce-widget" vs an open "add-widget"), it slipped past and filed a second tracker issue. A semantic guard now runs *after* the slug guard passes: it embeds the proposal's title+body and each open (or recently-closed, within the same 30-day window) tracker issue via the local embeddings provider, and suppresses the proposal when a candidate's cosine similarity meets a conservative floor (`SEMANTIC_DEDUP_THRESHOLD = 0.9`). It's strictly best-effort and self-gating: when no embeddings provider is configured (or the proposal embed is skipped/fails) the check reports `available:false` and filing proceeds — slug dedup already guarded the exact case, so an unavailable semantic layer only restores prior behavior, never files a slug-duplicate. No cold-bootstrap risk (embeddings degrade to a no-op with no provider, and this only runs inside the user-enabled scheduled sweep), no new config field, and plan-tracked slug-only issues are simply not embeddable so they're skipped. Candidate embedding is capped per run (`SEMANTIC_DEDUP_MAX_CANDIDATES = 50`) so a large backlog can't fan out unboundedly, and the shared `isIssueWithinDedupWindow` helper keeps the semantic and slug guards agreeing on which issues still count. (`server/services/layeredIntelligence.js` `checkSemanticDuplicate`/`findSemanticDuplicate`/`cosineSimilarity`, `server/services/autonomousJobs/layeredIntelligenceHandler.js`)

## Creative Writing Quality Engine

- **[issue-2181] Prose series can now be exported as a compiled manuscript, an ePub, or a print-interior PDF.** A prose-format series had no shipping step — once the quality-engine phases produced publishable prose the pipeline dead-ended at "text in stages" (comic series already had `comicPdf`/`volumePdf`, prose had nothing). A new **Export** tab on the series page (deep-linkable `/pipeline/series/:id/export`, registered in the nav manifest) offers three downloads, each assembled on demand and streamed straight to disk: (1) a **compiled manuscript** (Markdown with YAML front matter, volume breaks, and chapter headings) that reuses the same arc-ordered, drafted-prose-only `collectManuscriptSections` stitching the editorial passes use, so the exported prose matches what the editor and LLM see; (2) an **ePub** — an OCF container (mimetype + container.xml + OPF manifest + nav doc + per-volume XHTML chapters + CSS + the cover image from the existing cover stage) packaged in-repo with the existing `zipWriter.createZip` (no epub dependency added, per the dependency policy — `mimetype` stays entry #0, stored, per spec); (3) a **print-interior PDF** — a trade-format interior (trim size, margins, running heads, title page, chapter openers) built on the same `pdf-lib` plumbing as `volumePdf.js`. Per-series export settings (trim size, interior font, title-page/subtitle/author/copyright/dedication) persist on the series record under a new `exportSettings` field (`sanitizeProseExportSettings`, wire-gated at `pipelineSeries` v12 + `ADDITIVE_SERIES_FIELDS` so an older peer can't strip-then-LWW it). Issues group into volume breaks by `seasonId`. (`server/services/pipeline/proseExport.js`, `server/lib/proseExportSettings.js`, `server/routes/pipeline/proseExport.js`, `client/src/pages/PipelineExport.jsx`)
- **[issue-2165] Editorial review now catches recognizable "AI slop" writing tells automatically.** Four new deterministic editorial checks join the existing 68: a tiered banned-word scan (hard-bans like "delve"/"myriad"/"utilize"; suspicious words like "robust"/"seamless" only flagged when 3+ cluster in one paragraph), fiction-specific AI-tell idioms ("a sense of dread", "couldn't help but", "eyes widened", "a wave of relief washed over him"), rhetorical structural tics ("not just X, but Y", runs of "did not [verb]", punchy three-sentence-in-a-row bursts like "Fast. Precise. Deadly."), and structural burstiness (em-dash overuse, essay-style transition-word openers, mechanically uniform paragraph lengths). Each surfaces as an editorial finding with the exact offending quote, same as the existing prose checks — configurable thresholds and house-style allowlists included.
- **[issue-2166] Editorial review now flags an issue whose narrative voice statistically drifts from the rest of the series.** A new deterministic `style.voice-drift` check measures each issue's prose fingerprint — sentence-length rhythm (mean, spread, variation), fragment and long-sentence rates, paragraph shape, dialogue ratio, em-dash rate, abstract-noun and simile density, and how often sentences open the same way — then compares every issue against the series average and flags the outliers (e.g. "Issue 7's sentence-length variation is 0.18 vs the series 0.41 — prose has gone metronomic"). It's the cheap, deterministic complement to the existing LLM voice-consistency check: no model cost, and it names the exact issue and metric that drifted. You can tune the sensitivity (σ threshold), and optionally declare vocabulary "wells" — register categories like `trade: forge, anvil; body: pulse, sinew` — so it also flags an issue that drops a register the series leans on. It stays off until at least 4 issues are drafted (with fewer, the statistics are too small to distinguish real drift from a normal series).
- **[issue-2172] The draft prompts now carry a craft anti-pattern corpus, so first drafts avoid recognizable "AI prose" tells before the editorial pass ever runs.** A shared `craft-anti-patterns` prompt block — injected into the prose-draft stage and the Writers Room live-continuation suggestions — encodes hard rules the model must follow while writing: no triadic sensory lists, a cap on "did not [verb]" and "the way X did Y" similes, no "thought about X" constructions or over-explaining after showing, varied paragraph lengths, at least 70% in-scene (not summary), speech that sounds like speech, and at least one genuinely surprising moment per chapter. The same block carries "Stability Trap" countermeasures (from the Rettberg & Wigers 2025 finding that AI stories favor stability over change): characters must end truly different, bad things are allowed to stay bad, some losses are irreversible, information is withheld rather than dumped, and every real choice has a real cost. Preventing these in the draft is far cheaper than detecting them in editorial. Existing installs auto-upgrade their prose/writers-room prompts (customized prompts are left untouched and flagged).
- **[issue-2172] Series arc planning now emits a foreshadowing ledger that the Chekhov's-guns editorial check consumes directly.** When you generate a series arc overview, the model now plans 2–6 planted seeds as explicit `plant → reinforce → payoff` entries with the payoff landing at least 3 issues after the plant (so the reader has time to forget before the gun fires). The ledger persists on the series arc record (lockable and preserved across regenerates, like the reader map and ticking clock), and the `chekhov.setups-payoffs` check reconciles the manuscript against these author-declared seeds instead of inferring every setup from scratch — a more grounded, less hallucination-prone review.
- **[issue-2194] The voice-drift engine now has a dedicated matrix view showing every issue's full fingerprint, not just the flagged outliers.** A new deep-linkable **Voice Fingerprint** page (`/pipeline/series/:seriesId/voice-fingerprint`, linked from the series header and reachable via ⌘K / voice) renders the whole issues×metrics matrix as a monospace table — one row per drafted issue, one column per fingerprint metric (sentence rhythm, fragment/long-sentence rates, paragraph shape, dialogue ratio, em-dash rate, abstract-noun/simile density, dominant opener, plus any configured vocabulary wells) — with each statistical outlier cell highlighted in amber and a series mean/σ footer. Where the `style.voice-drift` editorial finding only names the flagged outliers in prose, this view shows the full picture at a glance, reading the same per-series config (drift threshold, min issues, vocabulary wells) as the check. Read-only, no LLM cost.
- **[issue-2168] A new "adversarial cuts" editorial check and mechanical applier tightens prose by identifying what a ruthless editor would cut.** The `prose.adversarial-cuts` check asks an LLM editor persona to cut 8–12% of the text as 10–20 passages, classifying each cut by type: FAT (purple prose), REDUNDANT (repeated information), OVER-EXPLAIN (spelling out what's clear), GENERIC (stock phrases), TELL (emotion-telling after showing), or STRUCTURAL (scenes that don't advance the story). Each cut lands as a manuscriptReview finding with the exact verbatim quote. The Manuscript Editor gains an "Apply safe cuts" action that batch-removes OVER-EXPLAIN and REDUNDANT cuts mechanically — exact string match with whitespace-normalized fallback, refusing ambiguous or too-short quotes — through the same snapshotted stage-write path fixes use (full undo via version history). The check also reports `fat_percentage`, `tightest_passage` (protected), and `loosest_passage` for a quick health read.
- **[issue-2170] A simulated four-persona reader panel now reads your whole series and shows you where the readers disagree.** Convene the panel from a new **Reader Panel** tab on the Reader Map page (deep-linkable via `?tab=panel`) and four distinct readers — The Editor (prose texture, subtext, over-explaining), The Genre Reader (pacing and page-turn pull, "gets bored by beautiful prose that doesn't GO anywhere"), The Writer (structure, beats, foreshadowing payoff, whose worst insult is "I can see the outline"), and The First Reader (pure emotional response, no craft vocabulary) — each read a condensed, content-hash-pinned arc digest of the series (per-issue summary, opening/closing excerpts, and notable dialogue) and answer the same ten qualitative questions (where momentum is lost, is the ending earned, what to cut, a missing scene, the thinnest character, best/worst scene, would-recommend, what haunts you, would-read-the-next-book), citing specific issue numbers. The editorial signal is where they *disagree*: an issue flagged by 3+ personas is routed straight into your editorial-findings triage flow as a consensus concern; softer some-flagged-some-didn't calls and "polarizing" issues (one persona's best scene is another's worst) are surfaced for your judgment. One LLM call per persona, run only from the explicit Convene button — nothing fires on its own.
- **[issue-2175] Worldbuilding craft doctrine now shapes universe generation up-front, and two new editorial checks verify it.** The universe-expansion prompt now injects Sanderson's-Laws craft doctrine before the writer model drafts — limitations over powers, costs that drive plot decisions, solutions foreshadowed before they resolve, iceberg depth, interconnected systems, 2–3 societal implications per major system, and a sensory signature per location — so autonomous and interactive runs alike start from a lived-in world instead of decorative set dressing (Series Autopilot calls the same stages, so autonomous mode is upgraded automatically). Two new series-scope editorial checks close the loop on the review side: `world.unforeshadowed-solution` (a plot problem solved by a rule/power the reader was never shown — the worldbuilding sibling of the deus ex machina) and `world.cost-free-power` (an ability used at a decisive moment with no cost or limitation on the page). Both reconcile the prose against the established world canon and the continuity-bible world-rule facts, so a rule that WAS planted earlier is not flagged. (First slice of #2175 — the character-framework and structure-rule pillars ship separately.)
- **[issue-2175] Prose drafting now carries Le Guin's craft doctrine — "style is not ornament, it IS the story" — so first drafts reach for strong verbs instead of adjective-noun clichés.** Following the worldbuilding-doctrine (#2219) and character-framework/structure (#2245) slices, the prose-writing stages (prose, comic script, teleplay) now inject a short, always-on prose-craft block ahead of drafting: style is the reader's experience of the world, not decoration over it; prefer specific nouns and verbs to adjective/adverb padding and cut dead clichés ("ancient wisdom", "piercing gaze", "heavy silence"); let concrete sensory detail carry meaning rather than summarizing an emotion; and vary sentence length and rhythm on purpose. It rides the existing `{{series.styleNotes}}` template variable (via `composeStyleNotes`), so there's no new prompt-template variable and no migration — and it's scoped to the drafting stages only, staying out of the structural beat-sheet/arc/episode-seed passes where sentence-level craft would just be noise. This completes CWQE Phase 10 (#2175).
- **[issue-2179] A series style guide can now carry concrete voice exemplar and anti-exemplar passages — "the tuning fork" — that get injected into every draft and revision prompt.** Adjective lists ("witty, atmospheric") anchor prose voice far more weakly than an actual passage does, so the series style guide gains two new fields: `voiceExemplars` (1–3 short passages that nail the target voice, each with an optional one-line note on what it demonstrates) and `voiceAntiExemplars` (passages in the wrong register kept as negative examples, noted with what's wrong — "too ornate", "wrong emotional temperature"). Add and edit them inline on the series style-guide editor. On generation they render into the house-style block as a "MATCH this voice — echo their rhythm, diction, and register" block and a "NEVER drift toward this" block — conditional, so a guide with no passages injects nothing. They ride the existing `{{series.styleNotes}}` template variable (via `composeStyleNotes`), so prose drafting, revision, arc planning, and episode-seed passes all honor them with no new prompt-template variable and no migration. The sanitizer drops empty passages, trims each to ~2000 chars, and caps the list at 3 so the fixed per-call prompt overhead stays tight. (First slice of #2179 — the AI-assisted voice-discovery flow, Writers Room parity, and the voice-fingerprint baseline hook ship separately.)
- **[issue-2178] Canon can now be reveal-gated so a mystery or twist doesn't leak into the prose before it's due.** Canon context — including character secrets and late-story facts — was injected into every issue's drafting prompt, so the writer model could (and would) spill a reveal issues early. Each canon entry (character, place, or object) now carries optional reveal gating: a `revealIssue` number hides the fact from any issue before it; a `spoiler` flag hard-hides it from all drafting context; and an optional `surfaceDescriptor` is the spoiler-free stand-in shown before the reveal ("the locked east wing" instead of "the wing where the heir is imprisoned"). The drafting/rewrite prompts (`buildStageContext`, the alternate-POV rewrite cast) filter canon to each issue's reveal horizon, while the judge and editorial checks still receive the full canon so they can verify hint placement. Absent gate fields mean "always visible," so every existing canon entry and every federation peer round-trips unchanged. A reveal-timing editor with a spoiler badge lands on every canon-entry card (universe + series). A new `continuity.premature-reveal` editorial check (gated on the series having authored any reveal-gated canon) scans drafted prose for a gated fact stated or unambiguously implied before its reveal issue, distinguishing a genuine leak from deliberate foreshadowing — and its reveal issue feeds the Chekhov setups-payoffs check as an authored payoff so an orphaned reveal is flagged too.
- **[issue-2177] Prose drafts now flow across issue boundaries instead of each issue opening and closing in a vacuum.** When drafting an issue's prose, the generator now injects the previous issue's actual closing prose (its last ~2000 characters) and the next issue's opening beats into the draft prompt — with guidance to open so it flows from the previous ending (without repeating its closing image or re-explaining what the reader just read) and to land the ending so it hands off to the next issue's beats. This is what keeps the narrative voice carrying across units and prevents the "every issue opens/closes the same way" tic, improving the autonomous first draft so the editorial loop has less to fix. Both blocks are token-budgeted (they degrade by trimming on a small-context local model rather than erroring), and when the previous issue hasn't been drafted yet — non-linear or parallel generation, or the first issue of a volume — the block simply doesn't render, so there's never any fabricated continuity. Every full prose (re)generation path gets it: the manual draft button, the auto-runner, and Series Autopilot. Existing installs auto-upgrade their prose prompt (a customized prompt is left untouched and flagged).
- **[issue-2179] The voice-drift check can now measure each issue against the voice you CHOSE — its style-guide exemplars — instead of the average of what got drafted.** The `style.voice-drift` check flags an issue whose prose fingerprint sits more than a threshold's σ from a baseline; that baseline used to be the drafted-issue mean, which has a blind spot — if every issue drifted the same way, the average drifts with them and nothing flags. A new **Drift baseline** setting on the check adds two modes on top of the default `drafted`: `exemplars` re-centers every metric on the combined fingerprint of the style guide's voice exemplars (the "tuning fork" passages from the first #2179 slice), and `blended` uses the midpoint of the two. In exemplar/blended mode the spread (σ) is still the drafted corpus's — only the center shifts — so an issue is judged against the chosen voice while still tolerating the series' natural per-metric variance. When the style guide has too little exemplar prose (under ~40 words combined) the run silently falls back to `drafted` and says so. Findings read "…vs the style guide's chosen voice of 5 words" instead of implying drift from the drafted mean, and the deep-linkable **Voice Fingerprint** matrix view renders the chosen-voice baseline as its own footer row with the outlier cells measured against it. The check now also lists the style guide as a source, so editing an exemplar re-stales the finding — exactly like its LLM siblings. (Third slice of #2179 — the voice-fingerprint baseline hook; the AI-assisted voice-discovery flow and Writers Room parity ship separately.) (`server/lib/editorial/voiceFingerprint.js`, `server/lib/editorial/checks/proseStyle.js`, `server/services/pipeline/voiceFingerprint.js`, `client/src/pages/PipelineVoiceFingerprint.jsx`)
- **[issue-2248] The chosen-voice baseline now also catches drift the whole series shares — not just per-issue outliers.** The exemplar/blended `style.voice-drift` baseline (#2247) flags an issue that stands apart from the chosen voice, but it had a residual blind spot: when *every* drafted issue sits at the same value on a metric (zero spread) yet that shared value is far from the chosen voice, there is no per-issue outlier to name — the whole corpus is uniformly off-register. The classic case is a dialogue-free series measured against a dialogue-heavy chosen voice: every issue's dialogue ratio is 0, so the per-issue z-score model stays silent even though the register is plainly wrong. The check now emits a distinct **series-wide** finding for exactly this case ("The whole series sits at 0% on dialogue ratio vs the style guide's chosen voice of 42.5% — the corpus is uniformly below the chosen voice here"), gated on a scale-free relative distance so it works across metrics with wildly different scales and never fires under the default `drafted` baseline (where the center is the mean by definition). Series-wide findings carry no issue number (it's a corpus-wide observation, not an outlier) and escalate one severity rank above the per-issue floor, and the **Voice Fingerprint** matrix view surfaces them as a banner above the table since they have no single cell to highlight. (Follow-up slice of #2179.) (`server/lib/editorial/voiceFingerprint.js`, `server/lib/editorial/checks/proseStyle.js`, `server/services/pipeline/voiceFingerprint.js`, `client/src/pages/PipelineVoiceFingerprint.jsx`)

- **[issue-2179] "Discover voice" now writes the same scene beat in five distinct registers so you can pick the series voice by ear.** Filling in a voice exemplar from a blank page is the hard part — so the series style-guide editor gains a **Discover voice** button that runs one LLM call and returns the *same* invented scene moment written five ways (spare / lyric / wry / close-psychic / cinematic). Each candidate renders side-by-side with its register label; one click files it into `voiceExemplars` ("the tuning fork") or, for a passage in the wrong register, into `voiceAntiExemplars` — both capped at 3, with the register carried as the passage note. Nothing persists until you save, exactly like the New Series "Generate with AI" concept flow, and the call only fires on your explicit click (no cold-bootstrap LLM work). The trial passages are seeded with the series' premise + existing tone/house-style so the registers vary *within* the world rather than swapping genre. Ships as a new `pipeline-series-voice-discover` stage prompt (seeded to existing installs, no migration since it's a brand-new template) behind `POST /api/pipeline/series/:id/discover-voice`. (Fourth slice of #2179 — the phase headline; Writers Room parity ships separately.) (`server/services/pipeline/seriesVoiceDiscover.js`, `server/routes/pipeline/series.js`, `server/lib/styleGuide.js`, `client/src/pages/PipelineSeries.jsx`, `data.reference/prompts/stages/pipeline-series-voice-discover.md`)

## Human Activity Tracking

- **[issue-2150] Activity timeline** — a new `/timeline` page shows a unified, day-by-day view of your activity (emails sent/received and calendar events, with more sources coming), backed by a machine-local `human_activity_events` store that your existing message and calendar syncs populate automatically. Pick any day (deep-linkable via `/timeline/:date`), see an hourly activity histogram and per-source tallies, and browse the day's events. Only metadata and a short summary line are stored — full message bodies stay in their per-source caches — and the timeline is machine-local (never federated to peers), like Tribe.
- **[issue-2151] iMessage ingestion (macOS)** — a new Settings → iMessage tab lets you pull your Mac's Messages history into the activity timeline and your Tribe touchpoints. It reads `~/Library/Messages/chat.db` read-only (never writing to Apple's database) using a built-in SQLite reader — no new dependencies — and matches conversations to the people you track by phone number (a new phone field on Tribe people, matched in E.164 form) as well as email. It's off by default and opt-in: enable it, set a sync interval, and use "Check setup" to confirm PortOS has macOS Full Disk Access (with a clear fix if it doesn't) or "Sync now" to run immediately. Only conversation metadata and a short preview line are stored — full message text stays in chat.db — and, like Tribe, nothing federates to your peers. Re-syncs are incremental and idempotent, so running it repeatedly never double-logs.
- **[issue-2160] WhatsApp import direction inference** — the WhatsApp chat backfill importer now takes an optional "your name in this chat" field. When set, messages from a sender matching that name land as `message.sent` events and everyone else as `message.received` (the same kinds email uses); left blank, messages stay neutral `message` events as before. The preview shows the sent/received split before you commit. Direction isn't part of the dedupe key, so an earlier neutral import isn't double-counted — clear the source's events and re-import to apply the classification.
- **[issue-2160] WhatsApp import optional chat label — hardens the cross-chat dedupe collision** — the WhatsApp chat backfill importer now takes an optional "chat label" field. WhatsApp exports carry no stable chat id (the filename differs between a `WhatsApp Chat - Alice.zip` and the bare `_chat.txt` extracted from it), so the content-hash dedupe key was deliberately un-scoped — which meant two DIFFERENT chats that each contained a same-named sender sending an identical message at the same timestamp granularity could collapse into a single event. Supplying a chat label (e.g. "Family group") now folds that stable, user-supplied scope into the dedupe hash so distinct chats key apart, and the label also becomes the displayed chat title. Left blank, the dedupe key is byte-identical to before, so existing imports and their idempotency are unchanged; use the same label consistently for a given chat across re-imports. (`server/services/whatsappImport.js`, `server/routes/timeline.js`, `client/src/services/apiTimeline.js`, `client/src/components/timeline/WhatsappImportPanel.jsx`)

## Privacy Center

- **[issue-2140] Encrypted PII Vault** — PortOS can now store your identity facts (legal name, addresses, phones, emails, DOB, ID documents, financial-account stubs) in an encrypted-at-rest vault: values are AES-256-GCM encrypted with a key auto-provisioned into your `.env` on first use, the API only ever shows masked values (like `••••6789` or `j•••@example.com`) unless you explicitly reveal one, and sensitive document types can never be marked for use in the upcoming data-broker scans. This is Phase 1 of the Privacy Center (epic #2138) — the UI arrives in Phase 3.
- **[issue-2141] Trusted Organizations registry** — the Privacy Center now has a place to track every organization that has (or had) your PII — banks, utilities, government, employers, subscriptions, medical, insurers, platforms — each with a trust stance (trusted/tolerated/unwanted) and per-org holdings linking to the exact vault records they hold. Holdings always show masked values, never plaintext, and deleting an organization or a vault record automatically cleans up its holdings. This is Phase 2 of the Privacy Center (epic #2138) — it's the data backbone for the upcoming change-of-address inventory and "who has my PII" view; the UI still arrives in Phase 3.

## Songs

- **[issue-2109] The live vocal Tuner now locks onto the note you're singing instead of flickering.** Holding a steady note used to make the readout strobe — the note label flipped between neighbors, the needle lurched, and the display dropped to "—" on every breath or consonant. The tuner now holds a stable reading through brief dropouts (dimming while it coasts), only blanks after a real pause, keeps one note label while your pitch wanders up to ~60¢ around it, and re-locks promptly when you actually change notes — so a held note shows one label with a gently moving needle.

## Rounds

- **[issue-2104] Fixed: 500 Miles' reference TikTok videos now show up on older installs.** The built-in "500 Miles" round shipped with three TikTok reference performances, but any install that already had its data saved from before those were added never picked them up — the Reference material section stayed empty. A one-time backfill now restores the three references for those installs, without touching anything you may have added of your own.
- **[issue-2105] Fixed: "Hey Ho Nobody Home" now sings in D minor and stacks cleanly with its partner rounds.** The built-in round shipped with a mis-transcribed melody — centered on G with a major third — that contradicted its own key label and sounded a harsh clash when stacked with Ah Poor Bird and Rose Rose Rose Red in the Round Stack view. The melody is now the correct D-centered, all-natural minor tune (same shape, just the right key), so the three-round quodlibet is consonant. Older installs get the corrected melody automatically; a score you customized yourself is left untouched (use "Refresh from template" to pull the fix).
- **[issue-2106] Learn the real parts from a reference recording.** A round reference (like a layered TikTok performance) can now carry attached audio — upload a file or capture through the mic while the video plays — and open in a new analysis view: see the waveform, mark the time ranges where each voice sings alone, and extract each range into a proposed part snapped to the round's tempo and key. Review the proposal side by side with the part you already have, with each bar flagged where the notes disagree, then apply it with one click (nothing changes until you Save). All analysis runs locally on your machine — no AI calls.
- **[issue-2120] Attach a reference's audio straight from a URL.** Following #2106's upload + mic-capture attach paths, a reference now also offers "Download from URL": paste a link (a TikTok performance, a YouTube clip, …) and PortOS downloads + extracts just the audio via `yt-dlp` into your uploads dir, ready to analyze like any other attached recording — with a live download-progress readout you can cancel. It's a best-effort convenience (scraping is brittle; upload and mic capture stay the primary paths) and requires `yt-dlp` installed on the host (detected at runtime, no new npm dependency — the same discovery the YouTube track import uses). The URL is SSRF-guarded (http(s) only; loopback/link-local/private-LAN hosts refused) and the download is bounded to a short-clip size/duration. Under the hood, the yt-dlp download+extract+SSE-progress core is now shared between this and the YouTube track import (#1945) rather than duplicated. (`server/services/ytdlpAudioImport.js`, `server/services/roundReferenceAudioImport.js`, `server/services/trackYoutubeImport.js`, `server/lib/fileUtils.js`, `server/routes/rounds.js`, `client/src/hooks/useReferenceAudioImport.js`, `client/src/components/songs/ReferenceAnalysis.jsx`)

## Media

- **[issue-2196] Image-to-Video sizing now accepts custom width/height inputs.** The Video Gen page's resolution picker showed only preset aspect ratios — users who wanted an exact I2V size had to set it via URL params, remix, or wait for the auto-snap from an uploaded image. The resolution field now includes editable width and height inputs directly below the preset dropdown, accepting any value (the server floors to a 64-pixel grid and enforces the FFLF pixel budget as before). Picking a preset, typing a custom value, or auto-snapping to a source image all update the same state, and the "custom" option appears in the dropdown whenever your dimensions don't match a preset.
- **Installing a video LoRA from HuggingFace now shows live download progress.** The LoRA Manager's "Install video LoRA from HuggingFace" form (and the curated video quick-install buttons) used to freeze on a static "Downloading…" label with no indication of how far along a multi-GB LoRA was — unlike image-model downloads, which show a percentage. The install now streams byte-level progress over SSE: the button shows `Downloading 42%` and a progress bar with `received / total` bytes fills as it downloads (an indeterminate pulse when the server can't read a Content-Length). Errors — including the "couldn't detect the model family" case that offers the inline "Install as LTX-Video" retry — still surface exactly as before.
- **The LoRA Manager's Installed list is now grouped by Video / Image.** The curated "Video LoRAs" suggestion cards sat directly above a single flat "Installed" grid, so installed *image* LoRAs looked like they belonged to the video section. The Installed section is now visually divided from the suggestions and, in the "All" view, splits into labeled **Video LoRAs** / **Image LoRAs** subsections (mirroring the suggestion panel) so it's obvious which renders each LoRA applies to. The Image/Video filter still collapses it to a single scoped list.
- **[issue-2036] Sketch & Annotation Canvas (phase 2): re-render an image guided by your annotations.** The annotate page (`/media/annotate/:mediaKey`) gained a "Re-render" action: draw over a generated image, then feed your markup back through local img2img so the marks reshape the render. A confirmation dialog names the exact local model it will run (no surprise AI calls) and lets you add an optional prompt and tune how much to change before it starts; the new render is queued and appears in Media History. Requires a local FLUX img2img runner — the action explains when one isn't available. Phase 3 (blank-canvas storyboard) remains open on #2036.
- **[issue-2036] Sketch & Annotation Canvas (phase 3): blank-canvas storyboard sketches on pipeline scenes.** Each scene in the Storyboards stage now has a **Sketch** button that opens a blank drawing surface — the same draw/erase/undo/color/size toolbar as the image annotator, but starting from an empty white canvas instead of over a generated image. Rough out a composition or shot idea for the scene, save it, and a thumbnail appears inline on the scene; the button becomes **Edit sketch** to reopen the same canvas. The sketch is deep-linked and reload-safe (`/media/annotate/sketch:<id>`) and returns you to the storyboards stage when you're done. Blank sketches are stored under a `sketch:` key kept local to the media-sketch service — deliberately outside the shared media vocabulary so they never leak into collections, cross-machine sync, or the media catalog. Closes #2036. (`server/services/mediaSketches.js`, `server/routes/mediaSketches.js`, `client/src/components/media/AnnotationCanvas.jsx`, `client/src/pages/MediaAnnotate.jsx`, `client/src/components/pipeline/stages/StoryboardsStage.jsx`)
- **[issue-2199] LoRA downloads are now verified for corruption before install.** A truncated or corrupt `.safetensors` download from Civitai or HuggingFace used to install silently and then render garbage ("mosaic") output at generate time. PortOS now checks every freshly downloaded LoRA's structure (safetensors header + size) and, when Civitai provides a SHA-256 for the file, compares it against the bytes on disk. On any mismatch the partial file is deleted and the install fails with a clear error so you re-download instead of training or rendering against corrupt weights.
- **[issue-2196] Video Gen now takes an exact custom Width×Height.** The `/media/video` page only offered a 9-item resolution preset dropdown, so a size the presets didn't cover was only reachable via URL params, remix/continue, or aspect-snap on upload. Two numeric Width and Height inputs now sit beside the preset select — type any dimensions and the render uses them (the server still rounds each down to a multiple of 64 and enforces the per-tier pixel budget). Uploading a source image still auto-snaps the size to the image's aspect ratio by default, until you take the dimensions into your own hands.
- **[issue-2197] First-run model downloads now show GB progress instead of raw text.** The video and image generation handlers now parse byte sizes from `DOWNLOAD:` markers and tqdm progress bars, displaying a formatted "Downloading model · first run · X.X GB / Y.Y GB" message instead of the raw protocol output. Progress frames include `downloadedBytes`/`totalBytes` numeric fields so the UI can build download progress bars. Phase tracking (`isDownloading`, `currentPhase`) ensures the formatting only applies during actual download phases.
- **[issue-2198] A wedged local video render that produces no output is now killed automatically instead of pinning the GPU queue.** The existing completion watchdog only guards a render that finishes and then hangs in teardown — it never armed for a render that stalls *before emitting anything* (a known MLX/Metal failure mode where the job never exits and never prints), which held the serialized GPU lane until you manually canceled. A new idle-stall deadline arms at spawn and resets on every child output line (stdout or stderr); if a render produces no output for a generous window (default 10 minutes, tunable via `VIDEOGEN_IDLE_STALL_MS`) it's SIGKILLed and surfaced as a failed job, so the next queued render can start. The window is deliberately generous so a legitimately slow first-token render (weights load + Metal kernel compile) is never killed, and a manual cancel still works and always wins. (`server/services/videoGen/local.js`)
- **[issue-2124] Add and manage custom image/video base models from HuggingFace — no more hand-editing a JSON file and restarting.** Base models used to be addable only by editing `data/media-models.json` and restarting the server, while LoRAs had a first-class "install from HuggingFace" flow. The Media Models page (`/media/models`) now has that same self-service experience for base models: paste a HuggingFace repo (URL or `org/name`), and PortOS inspects its files + model card to detect whether it's an image or video model and which runtime loads it, then appends a registry entry and hot-reloads it live (no restart). The catalog lists every built-in model read-only alongside your added ones, which you can rename, retune (steps/guidance), or remove inline. Formats no runtime can load are **refused up front with a clear reason** — a GGUF-only repo (e.g. `unsloth/LTX-2.3-GGUF`), a Wan or HunyuanVideo repo (which need a dedicated BYO venv), or anything unclassifiable — so a bad add can never wedge the model picker. For a GGUF LTX build specifically, the refusal points you at the native MLX Q4 model PortOS already ships. The multi-GB weight download still happens through the existing per-model download badge on the gen form once the entry exists.

## MeatSpace POST

- **[issue-2099] POST correctness batch: fill-blank scoring, an honest adaptive preview, weighted session scores, and fair shuffles.** Memory Fill-Blank drills now score and count toward your spaced-repetition schedule/mastery like Memory Sequence and Element Flash — a scoring-shape bug meant a fill-blank answer could never be marked correct no matter what you typed, so any fill-blank work inside a scored session was silently unrecorded (also closes #2116). The Adaptive Difficulty preview for Multiplication now matches what a session will actually generate: it shows your current progressive-ladder rung when the ladder is on (the default) instead of a "Max Digits" adjustment that could never apply. Session scoring config gained real teeth — a per-domain weight you set now actually shifts your blended session score (uniform weights, the default, score exactly as before). Memory drill generators (fill-blank, sequence recall, element flash) now use an unbiased shuffle instead of a skewed one, so which lines/elements you're drilled on is genuinely random.
- **[issue-2101] POST config & UX polish: Morse in the sidebar, config presets, keyboard-accessible history, pausable transitions.** Morse is now a full nav entry under the POST sidebar section (previously reachable only via the launcher or ⌘K). The drill config page gained one-click presets — "Balanced daily", "Math focus", "Cognitive focus", and "Everything (local-only)" — plus per-group "Enable all / Disable all" buttons on the Mental Math, Cognitive, and LLM sections (bulk-enabling LLM drills is blocked until you've picked an AI provider). History session rows now expand/collapse with the keyboard (Enter/Space), not just a mouse click. The inter-drill transition screen gained a Pause/Resume control and pauses its 3-second auto-advance countdown while you're hovering or focused on it, so you're never yanked into the next drill mid-read.
- **[issue-2097] The Wordplay tab now tracks your practice.** Completing a round of Compound Chain, Bridge Word, Double Meaning, or Idiom Twist in the standalone Wordplay tab used to score every response and then throw the result away — closing the tab meant the practice never happened. Each completed round now logs to your training history (same log the in-session drills and streak use), so Wordplay accuracy shows up in training stats and counts toward your practice streak. The standalone tab and the in-session drill runner now also share one scoring path for these four types, and a bug that always logged 0% accuracy for in-session LLM-drill training rounds (regardless of actual performance) is fixed too.
- **[issue-2114] Wordplay training-log entries now keep the per-question breakdown, not just the round total.** Follow-up to #2097: each completed Wordplay round — whether from the standalone Wordplay tab or a wordplay drill inside an in-session training session — previously logged only the aggregate question/correct counts. It now also stores each question's prompt/response, score, feedback, and whether it cleared the correct threshold — so a future progress dashboard can trend which individual wordplay prompts you miss. Bridge Word's persisted prompt is now the clue set it actually showed you (it previously had no readable prompt at all). Entries without a breakdown (older rows, and other training modules) remain fully valid.
- **[issue-2094] Scores now separate how *accurate* you were from how *fast* — and stop rewarding not answering.** Every scored drill now records accuracy (of the answers you actually gave), completion (how much of a timed drill you reached), and average speed as distinct numbers alongside the blended headline score, so the session and history views show accuracy and speed side by side instead of one merged figure. Running out of time on a timed drill now lowers *completion*, not accuracy — an unreached question no longer counts as wrong. Adaptive difficulty follows your accuracy (with a guard that skips adjusting when you barely started a drill), so a fast-but-sloppy run and a slow-but-careful run now push difficulty in different, correct directions. The n-back drill is rescored with proper signal-detection: never pressing now lands at ~50% (chance) instead of the old ~70% freebie, and always-pressing is penalized for its false alarms. The reaction-time drill is now scored on your median reaction speed (false starts invalidate their trial) with your best time tracked too. Older saved sessions without the new fields still display and chart correctly.
- **[issue-2093] Session results now teach from mistakes on every drill, not just AI ones.** Expanding a math, memory, or cognitive (n-back, digit-span, Stroop, Schulte table, mental rotation, reaction time) drill row on the results screen used to do nothing — only AI-scored drills showed a review. Every drill row now expands into a per-question/per-trial breakdown: a "missed items" summary up top, then a type-appropriate view — a prompt/answer/expected table for math and memory, letter-sequence hit/miss/false-alarm marks for n-back, shown-vs-recalled digits for digit span, word/ink/your-answer for Stroop, and per-trial timing with slow-response outliers flagged for the pure speed drills. Unanswered questions are shown distinctly from wrong ones. The same review now also appears when you expand a past session in History.
- **Multiplication drills now ramp up instead of starting hard.** The mental-math multiplication drill used to open at a fixed 2-digit × 2-digit difficulty (e.g. `566 × 191`) for everyone. It now climbs a mastery-gated ladder — `1×1` → `1×2` → `1×1×1` → `2×2` → … — starting at single-digit × single-digit and advancing to the next rung only after you answer a level quickly *and* accurately (≥90% correct within a per-rung speed target). The drill header and the config page show your current rung and per-level mastery. On by default; turn off "Progressive difficulty" on the Multiplication card to go back to the manual Max Digits setting.
- **Morse trainer audio now works on mobile Safari.** iOS Safari starts the Web Audio context suspended and only unlocks it once `resume()` fully settles. The trainer fired `resume()` without awaiting it, so the first tones were scheduled against a still-suspended clock and never sounded on iPhone/iPad. It now awaits the resume before scheduling any tone (matching the app's other audio modules), so Copy, Head Copy, and Send-mode keying all produce sound on mobile Safari.
- **The Elements Song trainer is now mobile-friendly and speed-readable.** The periodic-table memorization view no longer overflows off the right edge of a phone screen: the mastery summary, search box, and Mastery/Category toggle now stack and stretch to full width on small screens, the periodic table uses smaller cells (with a scroll-hint fade so it's clear the grid pans horizontally), and the header shrinks to fit. It also gained a **Rapid Read** practice mode that RSVP-flashes the lyrics one word at a time (reusing the app's speed reader) — run the whole song from the Practice list, or tap the gauge icon on any verse to speed-read just that section.
- **[issue-2102] POST test-coverage hardening.** Added behavioral test coverage for previously-untested load-bearing paths: stats aggregation (module/drill averaging, the days-window cutoff boundary, empty-window shape, streak passthrough), the adaptive-difficulty preview and its integration across every math drill type, the drill-generation route's cache/memory/adaptive dispatch and error paths, all 14 LLM drill generators plus their local/LLM scoring branches, non-memory session scoring/rollup, and the memory-builder's chunk-mastery ordering and built-in mastery-only update restriction. The memory drill's fuzzy-match/fill-blank/sequence/hint scorers and the LLM/session-launcher drill runners' pure helpers are now exported and unit-tested directly instead of only through component rendering (test-only, no user-facing change).

## POST Training

- **[issue-2100] POST now tells you what to practice next — and works toward goals you set.** The launcher gained an **"Up next"** panel: a prioritized list of what to work on — memory items due for review, mastered skills due for a maintenance rep, your weakest recent skill, and laddered drills you're close to leveling up (e.g. "2 more clean reps to reach 2-back", "advance to Koch level 6") — each a one-click deep link into the exact drill or mode. You can set optional **goals** (daily minutes, sessions per week, streak length, Morse WPM) on the config page; the launcher and the dashboard's Daily POST widget show your progress against them, and everything hides cleanly if you set none. A **Full POST** now interleaves your drills across domains (math → cognitive → memory → verbal) instead of running all of one type in a row, and a **Quick 5 Min** session leads with your top recommendation instead of a purely random pick. As always, AI-backed wordplay drills are never auto-added to a default session — those stay opt-in.
- **[issue-2096] POST now checks whether you *still* know a skill, not just that you once did.** Element and chunk mastery is now decay-aware: it reads your most recent attempts (a rolling window) instead of an all-time tally, so a recent run of misses actually lowers your mastery and an unlucky early wrong answer no longer haunts you forever. On top of that, POST tracks the skills you've mastered but stopped practicing — multiplication rungs below your current level, cognitive rungs you've passed — and schedules a light "maintenance rep" for them on an expanding 7 → 30 → 90-day cadence. When one comes due, a Quick session quietly mixes in one or two labeled review reps; passing pushes the next check further out, while failing marks the skill "needs refresh" and re-checks it sooner (it never demotes a level you've earned). The Progress dashboard gains a Skill Retention panel showing each skill as fresh / due for review / needs refresh, plus a 90-day retention percentage. Nothing appears until you've actually mastered something, so fresh installs and existing data stay quiet until there's a skill worth re-verifying.
- **[issue-2098] POST sessions survive a refresh, are shareable by link, and can't double-record.** A POST session no longer lives only in the current page: refreshing mid-drill now resumes the same run (same drill queue, same completed results, right where you left off), and refreshing on the results screen keeps your results. Every saved session gets its own shareable, bookmarkable address (`/post/session/<id>`) — History rows open it, and it shows the same per-drill mistake review as the just-finished screen; a stale or deleted link shows a clear "session not found" message. Wordplay game modes now live in the URL too (`/post/wordplay/<mode>`), so a mode is deep-linkable and refresh-safe. Saving a session is now idempotent: if the network drops and the app retries, you get one record, not two — and a hiccup in the after-save bookkeeping (spaced-repetition scheduling for memory drills) can never fail an already-saved session.
- **[issue-2095] Cognitive drills now level up as you master them, instead of training at one fixed difficulty forever.** N-back, digit span, Schulte table, mental rotation, and Stroop each climb their own mastery-gated ladder: n-back ramps 1-back → 2-back → 3-back and then squeezes the flash speed (2500 → 2000 → 1600ms); digit span grows its span and adds backward recall; Schulte grows the grid 4×4 → 6×6; mental rotation and Stroop add trials. You advance a rung only after sustained high accuracy at the current one (n-back uses the fair signal-detection accuracy, so never-pressing can't bank a level), and you're never demoted below a rung you've earned. Each drill now stamps its difficulty into your saved results, so progress is tracked per level (a 2-back run no longer pools with a 3-back run). The config page shows each drill's current rung and progress toward the next, with a per-drill "Progressive" toggle (on by default) — turn it off to set the knobs by hand, including the previously-hidden n-back stimulus speed and digit-span show time. Reaction time stays a fixed measurement. On by default for existing installs with no migration.
- **[issue-2091] A unified Progress dashboard shows whether you're actually improving.** POST has a new **Progress** page (in the POST sidebar section, ⌘K, and voice nav) that pulls scored sessions, Morse/memory practice, and mastery into one view. It charts your score, accuracy, and response-time trends over time — so "am I getting faster?" finally has an answer — with same-day sessions rolled into one point (no more overlapping dots), a per-domain focus selector, and time-in-training cards. Your practice streak is now a single number counting *any* active day — a scored session **or** a Morse/memory practice — so the launcher, the Morse trainer, the Progress page, and the dashboard widgets all show the same streak instead of three different ones. A mastery panel shows your multiplication ladder rung and each memory item's mastery and due status. The full session-history table lives on as a sub-view.
- **[issue-2092] Morse progress now lives on the server, with WPM/accuracy trends and a per-character confusion matrix.** Your Koch level and every completed drill round are now saved server-side instead of only in the browser — so clearing browser data or switching devices (over Tailscale) no longer wipes your Morse mastery, and your level is identical everywhere. The first time you open the trainer it adopts your existing browser level once, then keeps both in sync. A new **Morse Progress** panel on `/post/morse` charts accuracy and effective-WPM trends per mode (Copy / Head Copy / Send), highlights your most-confused character pairs (E↔I and friends), and lists your weakest characters worst-first so you know exactly which to drill next. Send-mode rounds record per-character keying accuracy and timing too.

## Rounds

- **[issue-2121] Reference analysis can now pull a voice out of a stacked mix, not just solo spans.** The "analyze a reference's attached audio" workbench used to only transcribe time ranges where a voice sings *alone* — useless for the many layered TikTok builds where a new part enters over the layers already playing and never appears solo. Each segment now has a **Stacked** toggle: turn it on and set a short *backing reference* window from the moment just before the voice enters, and extraction subtracts that backing layer's spectrum (a hand-rolled FFT + spectral-diff, all pure local DSP — zero AI calls) to recover the newly-added voice's pitch line, then transcribes it onto the round's tempo/key grid exactly like the solo path. Enabling the toggle auto-seeds a sensible backing window ending right where the voice enters; you can nudge it or set it from the playhead. Solo extraction is unchanged.
- **[issue-2108] Built-in rounds now document their melody variant points.** Ah Poor Bird, Rose Rose Rose Red, and Zum Gali Gali carried melody/lyric choices that differ from other printed versions with no note explaining why — so a future accuracy check (or the AI evaluate feature) could mistake a deliberate variant for an error. Their notation now spells out each variant (Ah Poor Bird's C natural vs. the harmonic-minor C♯; Rose's verse and ending-note choices; Zum Gali Gali's D-minor transposition and refrain-only form) and cites the sources it was checked against.

## Dev Tools

- **[issue-1763] The Image Cleaner gained a CPU "Diffusion pass" step that actually disrupts SynthID.** Building on the Phase 1 composable pipeline (metadata/C2PA + median/sharpen toggles, #1768), the `/devtools/image-clean` page now offers a third opt-in step: a CPU-only diffusion pass (default OFF, lossy) that runs the same spatial round-trip as the ImageGen lightbox regenerate — resize-squeeze + micro color/contrast nudge + high-frequency perturbation (`applyLightRegen`) — perturbing SynthID's resolution-dependent watermark carriers. It runs synchronously in-request after the metadata and denoise steps (pipeline order: metadata → median/sharpen → diffusion), always re-encodes to PNG, and reports a fidelity metric (pixel-delta % + PSNR) in the per-step report so the UI can honestly say it *disrupts* the watermark rather than *removes* it (never verified against Google's detector). Options ride in the query string (`?diffusion=light`). The higher-reliability GPU FLUX img2img round-trip — which needs a non-gallery render seam factored out of the lightbox `/regenerate` flow plus async media-job progress — is a tracked follow-up; requesting it (`?diffusion=gpu`) returns an actionable 501 for now. The ignore-zone mask painter and resolution-aware GPU options also remain in that follow-up. (`server/routes/imageClean.js`, `client/src/services/apiMedia.js`, `client/src/pages/ImageClean.jsx`)
- **[issue-1763] The Image Cleaner's diffusion step gained an ignore-zone mask so you can preserve text and faces.** Diffusion — even the low-strength CPU pass — redraws the whole frame and garbles comic dialog, faces, and fine text. When the diffusion step is on, the `/devtools/image-clean` page now shows a preserve-region painter (brush + rectangle, with undo/clear and adjustable brush size + feather) over the source: you mark the regions the pass must not alter, and after the diffusion runs the server composites the ORIGINAL pixels back into those regions with a feathered (Gaussian-blurred) edge — a pure `sharp` round-trip (`compositeIgnoreZone`), no runner/GPU needed. The mask rides in the request as a no-dependency length-framed envelope (`<uint32 maskLen><mask PNG><image bytes>`, `?mask=1&feather=<px>`), and the per-step report labels the tradeoff honestly: a preserved region keeps its original SynthID locally, so the mask is a deliberate per-region quality-vs-disruption choice. The higher-reliability GPU FLUX round-trip and its resolution-aware options are tracked separately (#2264); requesting `?diffusion=gpu` still returns an actionable 501. (`server/lib/imageClean.js`, `server/routes/imageClean.js`, `client/src/components/media/IgnoreZonePainter.jsx`, `client/src/pages/ImageClean.jsx`, `client/src/services/apiMedia.js`)
- **The Usage page now shows your Claude Code subscription rate limits, not just PortOS's own token counts.** The `/devtools/usage` page previously only reported PortOS's internal AI-toolkit accounting (its own sessions/messages/tokens). It now leads with a **Claude Code Subscription** panel that surfaces the same numbers as the CLI's `/usage` command — current-session %, weekly % (all models and per-model, e.g. Fable), how much is left in each, and when each window resets — as color-coded meters that load on page open with a manual Refresh. Because there's no structured usage endpoint in the CLI, the server reads it the only way available: it runs `claude -p` with `/usage` on stdin (a read-only call that consumes zero tokens), parses the human-readable output into JSON, and caches it for 60s. Reset times render in the **machine's timezone** — resolved from the OS, not the server process's env — so a server running under PM2/systemd in UTC still shows the same reset times as an interactive `/usage`, instead of UTC-shifted ones. The numbers are a per-machine local approximation (they don't include other devices or claude.ai), and the panel says so. If the `claude` CLI isn't installed/signed-in, the panel shows a friendly note instead of erroring. (`server/services/claudeCodeUsage.js`, `server/routes/usage.js`, `client/src/pages/UsagePage.jsx`)

## Fixed

- **Scheduled improvement tasks no longer run twice.** When the periodic evaluation loop and the improvement-check timer fired close together, each took its own snapshot of the pending-task list, both found no matching task, and both queued an identical managed-app improvement (e.g. two overlapping `[Improvement: PortOS] Performance Analysis` runs). The `addTask` duplicate backstop that should have caught the second was silently disabled for these tasks: it read the candidate app only from top-level `taskData.app`, but queue-path improvement tasks arrive pre-built (`raw: true`) with the app in `metadata.app` — so the app-scoped dedup compared `null` against the existing task's `portos` and never matched. `addTask` now reads the candidate app from `taskData.metadata?.app` too, so the second concurrent queue is correctly rejected. (`server/services/cosTaskStore.js`)
- **PM2 standardization no longer overwrites a PM2 config PortOS didn't generate.** Standardizing an app used to always regenerate `ecosystem.config.cjs`, silently replacing a hand-written one — losing its custom ports and settings. A config PortOS didn't generate is now preserved by default (and its ports are left untouched in `.env`/Vite too); PortOS only regenerates its own previously-generated config. To deliberately replace a custom config, opt in with the overwrite flag when standardizing.
- **CoS TUI agents running `/do:release`/`/do:pr`/`/do:rpr` no longer get prematurely reaped as "done" while a reviewer is still working** — the idle-timeout reaper only special-cased the `/do:next --swarm` merge-queue wait (#2074); a slow multi-reviewer pass (e.g. codex reading a large diff) could go silent past the default 3-minute idle window and get finalized as a false `idle-complete` success before the release ever reached the merge gate, leaving the PR open with the task dashboard showing "completed" (observed on agent-61508f36, PR #2084, which sat open+unmerged for hours). Added a `createReviewLoopTracker` (`server/lib/tuiHandshake.js`) that latches on multi-reviewer-loop chrome — the review-plan status line (agent list in brackets), a review-pass counter (N of M), or the aggregate report heading — and extends the idle reaper's grace to 15 minutes while active — mirroring the merge-queue fix exactly, including surfacing a timeout as a needs-manual-finish failure (`reason: 'review-loop-idle-timeout'`) instead of a silent success. (Deliberately worded here without reproducing the literal trigger strings — this changelog entry itself would otherwise false-latch the tracker once a future release run reads it.)
- **[issue-2083] Catalog URL-ingest no longer over-refuses public pages that embed slow or cached iframes.** The pinned CDP SSRF gate (#2068) classified a top-level navigation using only `requestId === loaderId` — but CDP sets that on the main resource of *every* frame, including cross-origin sub-frames. A safe public article embedding a slow iframe (its document still in flight at settle-end) or a cached iframe (empty `remoteIPAddress`) was falsely refused. The gate now captures the top frame's `frameId` from the `Page.navigate` response and only that frame's document gates the pending/empty-IP checks; every connection IP is still independently verified, so the change can't weaken the gate — it only stops sub-frames from making refusal stricter. A defensive fallback retains the old `requestId === loaderId` classification if no request matches the top frameId. (`server/services/browserService.js`)
- **[issue-2107] A song scored in a minor key now shows the correct key signature.** Typing `key: D minor` (or `Em`, `F# minor`, `Bbm`, …) into a song's score header used to render the signature for the *parallel major* instead — e.g. "D minor" showed D major's 2 sharps on a staff full of natural notes. Minor keys now resolve to their relative major's signature (D minor → F, 1 flat), and the reference-audio-to-score feature spells accidentals on the correct side for minor keys too. Plain major keys ("D", "D major", "Dmaj") are unaffected.

## AI Providers

- **A wrong local-model default now self-corrects instead of failing the run.** When an Ollama or LM Studio provider is pointed at a model that isn't actually installed (a stale or mis-typed `defaultModel`, or a model you've since deleted), an API call against it returned a dead-end "model not found" — the user had to notice, open settings, and pick a real model by hand. The shared `callProviderAISimple` path now catches that error, lists the backend's actually-installed models, picks the best fit (the editorial recommender's family/size ranking, skipping embedding-only weights), repoints the provider's `defaultModel` (and any tier slot pointing at a missing model), tells the user via a low-priority notification + activity log, and retries the call once — so the run succeeds and the bad config is fixed going forward. Remote/API and CLI providers are left untouched, since their installed models can't be enumerated. (`server/services/localModelHealing.js`, `server/lib/aiProvider.js`)
- **[issue-2192] OpenCode TUI prompt paste no longer gets silently swallowed during slow startup.** When pasting a task prompt into a non-Claude TUI (OpenCode, Codex, Gemini), the paste could be sent while the TUI was still initializing — the prompt never appeared in the input buffer and the agent sat idle for 185s before being reaped as a false success. The TUI paste-commit path now verifies the pasted text actually rendered in the buffer (extracting a unique prefix from the prompt and checking for it after the paste-marker or fallback timeout). If verification fails, the paste is retried with exponential backoff (800ms, 1.6s, 3.2s); after 3 attempts the agent is finalized as a startup failure with a clear `paste-not-rendered` reason instead of a misleading `idle-complete` success. Claude TUIs already had positive input-ready detection and are unaffected. (`server/lib/tuiHandshake.js`, `server/services/agentTuiSpawning.js`)

- **Creative Director now evaluates rendered scenes on a local vision model instead of Opus.** Judging each freshly-rendered scene ("does it match the style spec and deliver the intent?") used to spawn a full Chief-of-Staff coding agent that landed on the active provider's *heavy* model — Opus for a Claude provider — purely to look at a couple of thumbnails and record a verdict. That decision now runs server-side through PortOS's vision-API path: it attaches the sampled frames to a **local, vision-capable model** (Ollama/LM Studio VLM) and parses a structured accept/retry/fail verdict, applying the exact same scene transitions (accept → add the clip to the collection; miss with retries left → re-render with a refined prompt; miss when exhausted → drop the scene). Pick the model under **Settings → AI Assignments → "Scene evaluation model"**; leave it blank to auto-select an installed local vision model. Installs with no local vision model configured transparently fall back to the original coding-agent path, so nothing breaks — but if you have a VLM installed, scene evaluation is now local and free. (`server/services/creativeDirector/sceneEvaluator.js`)

## Integrations

- **JIRA instances now support Jira Cloud (`*.atlassian.net`), not just Server / Data Center.** The JIRA client authenticated every instance with `Authorization: Bearer <token>` — correct for a Server/DC Personal Access Token, but Jira Cloud rejects that: a Cloud personal API token must be sent as HTTP Basic (`base64("email:token")`). PortOS now picks the scheme per instance by host — a `*.atlassian.net` base URL authenticates as Basic using the instance's email + API token (both already stored and required by the instance form), and any other host keeps the Bearer PAT — so Cloud and Server instances work side by side with no new fields or migration. Create a Cloud API token at id.atlassian.com → Security → API tokens and paste it into the instance's token field. (`server/services/jira.js`)
- **[issue-2235] Expired-token detection is now Cloud-aware, so a Jira Cloud token expiry gives the same friendly "regenerate your token" message as Server.** The JIRA client's expiry detector only recognized the Server/DC signal — a 200 response whose body is the HTML login page (`<!DOCTYPE`) — so a Cloud instance, which instead returns a JSON **401** on an invalid/expired token, never hit the friendly branch and surfaced the raw HTTP 401 instead. Detection is now instance-type-aware alongside the auth header: the `<!DOCTYPE` heuristic is gated to Server (so a Cloud JSON payload can't accidentally trip on the marker string), and a 401 from either instance type maps to one shared "token expired or invalid — regenerate your token" error. Both instance types now report expiry from one place. (`server/services/jira.js`)
- **Configuring an app's JIRA integration now auto-detects the workspace instead of hand-typing ids.** When you pick a JIRA instance + project in an app's JIRA settings, PortOS now detects that project's **boards** and offers them as a dropdown (`id — name (type)`) rather than a free-text "Board ID" field — the field where a board id silently went stale. If the app's saved board id isn't among the project's live boards (e.g. carried over from a Server instance after a Cloud migration, where ids are reassigned), it's flagged with an amber "pick a current one" warning; the selected board also shows its current **active sprint** as confirmation. The **Epic Key** field gained live search-by-name (type a name → pick from matching epics) and validates a typed/pasted key against the instance — confirming it resolves as an Epic, or warning when it's the wrong type or no longer resolves. New read-only endpoints back this: `GET /jira/instances/:id/projects/:key/boards` and `GET /jira/instances/:id/issues/:key`. (`server/services/jira.js`, `server/routes/jira.js`, `client/src/components/apps/EditAppDrawer.jsx`)

## Internal

- **[issue-2256] Removed the dead cos-runner `/run` devtools endpoint (latent Windows ENOENT bug).** The CoS runner's `POST /run` devtools path spawned a bare command under `shell:false` with the prompt passed as an argv — the same Windows `spawn ENOENT` bug the CoS agent path had (#2243), but no code called it: the live "Run Prompt" flow goes through `server/services/runner.js` (already fixed), and `executeCliRunViaRunner` + the `/runs`, `/runs/:id/output`, `/runs/:id/active`, `/runs/:id/stop` sibling endpoints and their client wrappers (`getActiveRunsFromRunner`, `isRunActiveInRunner`, `getRunOutputFromRunner`, `stopRunViaRunner`) were referenced only by their own tests. Rather than refactor dead code to be Windows-safe (a prompt-via-stdin rewrite to dodge the ~8191-char `cmd.exe /c` command-line limit), the whole devtools-run cluster is removed — the endpoint, its in-memory `activeRuns` map + `ensureRunsDir` helper, the `run:data`/`run:complete`/`run:error` socket forwarders, and the now-orphaned client functions and their tests. The main server and cos-runner sidecar ship in one repo and deploy together, so there's no cross-install compatibility concern. (`server/cos-runner/index.js`, `server/services/cosRunnerClient.js`, `server/services/cosRunnerClient.test.js`)

- **[issue-2249] The `issue-reconcile` zombie-scan is now forge-aware — it heals stranded issues on GitLab (`glab`) as well as GitHub (`gh`).** The v1 deterministic scan resolved the repo via `getOriginInfo().isGithub` and queried `gh` only, so enabling `issue-reconcile` on a GitLab-tracked app was a silent no-op. `server/services/issueReconcile.js` now resolves the forge from the app's git `origin` host inside `reconcile()` (github.* → GitHub, gitlab.* → GitLab, anything else → skip/park) and delegates to a per-forge state gatherer. Both gatherers normalize into ONE common issue shape (`{ number, title, url, labels, assignees }`) + change shape (`{ number, headRefName, body, url }`), so the pure classifier (`classifyIssue`), the boundary-safe ref matchers, and the `zombieSignature` convergence guard stay shared and unit-testable with zero forge branching — GitLab maps `iid`→number, MR `source_branch`→head ref (carries `claim/issue-<iid>`), MR `description`→body (carries `Refs #<iid>`). It remains a SINGLE task type (not three): the coordinator prompt is shared and simply gets the resolved forge + the matching `gh`/`glab` command table injected via `{zombieIssues}` (prompt bumped to v2, v1 preserved in `PREVIOUS_DEFAULT_PROMPTS` per the cross-install upgrade contract). A cwd-aware `glab` exec helper was added at `server/services/gitlab.js` (mirrors `execGh`). The JIRA sibling — a status-based "In Review with remaining scope" analog healed through the PortOS JIRA API rather than a forge CLI — is deferred to a follow-up (`Refs #2249`). (`server/services/issueReconcile.js`, `server/services/gitlab.js`, `server/services/cosTaskGenerator.js`, `server/services/taskPromptDefaults/`)

- **[issue-2183] Creative Tool Registry + gated dispatch (CDO Phase 1).** Foundation for the Creative Director orchestrator (epic #2182): a single registry (`server/services/creative/toolRegistry.js` + per-domain modules under `creative/tools/`) of creative-suite tools an orchestrating agent can call — universe, story builder, writers room, pipeline, media, catalog — each a thin conductor wrapper over an existing service entry point (`createUniverse`, `generateStep`, `startSeriesAutopilot`, `enqueueJob`, …), never a re-implementation. All orchestrator calls flow through one governed chokepoint, `dispatchCreativeTool(name, args, ctx)`, which validates args (Zod), enforces a new `creative` autonomy mode, charges the daily action budget for `llm`/`render` tools, and appends a `{tool, argsDigest, outcome, timing}` entry to the calling project's run ledger. The `creative` autonomy domain (`server/lib/domainAutonomy.js` `getCreativeAutonomyMode`) is kept separate from the four core domains and defaults to mirror the `cos` mode, so existing autopilot users get consistent behavior with no migration, while an explicit `domainAutonomy.creative` value can override it later. Direct user actions through the existing routes are unchanged — the gate lives only on the orchestrator path. No user-facing surface yet; the directive composer and plan board arrive in later CDO phases.

- **[issue-2220] Creative render tools with route-level persistence (CDO Phase 2 slice).** The Creative Director orchestrator can now render comic-issue and volume (season) covers — `pipeline_renderComicCover`, `pipeline_renderComicBackCover`, `pipeline_renderVolumeCover`, `pipeline_renderVolumeBackCover` are registered creative tools. These render jobs need more than a bare enqueue: the media-job filename hook attaches the finished image only if the target cover slot already carries the returned `jobId`, and that slot write used to live in the route factory (`routes/pipeline/covers.js#makeCoverRenderHandler`), so wrapping the bare `enqueueComicCover`-style service in a tool would silently drop orchestrated covers. The enqueue+persist flow (build the in-flight render slot, script-gate, write it onto the record) now lives in shared service entry points — `renderComicCover` / `renderComicBackCover` (→ `stages.comicPages.{cover|backCover}` via the serialized `updateStageWithLatest` series write tail) and `renderVolumeCover` / `renderVolumeBackCover` (→ `series.seasons[].{cover|backCover}` via `updateSeasonOnSeries`) in `visualStages.js` — so the four cover-render routes and the four orchestrator tools share ONE code path. The persist keeps the script-gate semantics (absent `coverScript` preserves the persisted concept, empty string clears it) and serializes against a concurrent blur-save on the same record. Tools charge as `render` against the daily action budget. Cover render is the primary deliverable; wrapping the remaining route-persisted render actions (comic page render / refine-render) as tools stays tracked on #2220.

- **[issue-2241] Creative comic-page render + refine-render tools with route-level persistence (CDO Phase 2 slice).** Completes the #2220 audit clause the covers slice (#2234) deferred: the Creative Director orchestrator can now render and refine full comic pages — `pipeline_renderComicPage` and `pipeline_refineComicPageRender` are registered creative tools. Like the covers, these need more than a bare enqueue — the media-job filename hook attaches the finished image only if `stages.comicPages.pages[pageIndex]`'s active variant slot already carries the returned `jobId`, and that slot write used to live in the route handler (`routes/pipeline/issues.js`), so wrapping the bare `enqueueVisualComicPage` / `refineComicPageRender` in a tool would silently drop orchestrated pages. The enqueue+persist flow now lives in shared service entry points — `renderComicPage` and `refineComicPageRender` (both persist onto `pages[pageIndex].{proofImage|finalImage}` through a shared `persistComicPageSlot` helper over the serialized `updateStageWithLatest` issue write tail) in `visualStages.js` — so the page render + refine-render routes and the two orchestrator tools share ONE code path, mirroring the `renderComicCover` / `makeCoverRenderHandler` structure exactly. The persist runs inside the write tail's compute step so a concurrent page edit or sibling render can't be reverted by a stale snapshot. Tools charge as `render` against the daily action budget. (`server/services/pipeline/visualStages.js`, `server/routes/pipeline/issues.js`, `server/services/creative/tools/pipeline.js`)

- **`getSettings()` now serves from an in-memory cache instead of re-reading settings.json on every call.** `getSettings()` is invoked 100+ times across the codebase (several per request), each previously doing a fresh filesystem read + JSON.parse of app-wide, rarely-changing data. It now memoizes the parsed settings, populated lazily on first read and refreshed off the `settings:updated` event that every `save()`/`updateSettings()`/`updateSettingsWith()` already emits — so app-driven writes keep the cache coherent while write paths still read fresh off disk inside their write queue for the read-merge-write, preserving write-ordering guarantees. Mirrors the existing `settings:updated`-invalidated cache in `sharing/annotationIdentity.js`. Alongside it, a few independent per-file read loops are now parallelized (`digital-twin-analysis.validateCompleteness`, editorial series-health's series+settings load) and hot page derivations memoized (`Review` grouping/sorting, `Tribe` care-queue), cutting redundant work on socket-driven re-renders.

- **[issue-2225] Test suites that write settings.json directly are now hardened against the `getSettings()` read-cache.** The cache added in the perf change above only refreshes off the `settings:updated` event `save()` emits, so a test that writes settings.json straight to disk (bypassing `save()`) can read a stale cache after a prior `getSettings()` warmed it. Two suites already patched this inline (`authGate.test.js`, `publicApiIntegration.test.js`); two more (`auth.test.js`, `settings.secretsStrip.test.js`) only passed because their `vi.resetModules()` incidentally discarded the cache after each write — an unasserted ordering that a future warm-then-direct-write would silently regress. A new shared test util `server/lib/settingsTestUtil.js` exposes `bindSettingsFile(dataRoot)` → `writeSettingsFile` / `mergeSettingsFile`, which do the disk write **and** the cache reset (via the required dynamic import of `__resetSettingsCache`, since a static import binds to a pre-`resetModules` instance) in one place. All four suites now route through it, so future direct-writers get correct invalidation for free. Footgun-removal only — no behavior change. (`server/lib/settingsTestUtil.js`, `server/lib/authGate.test.js`, `server/routes/publicApiIntegration.test.js`, `server/routes/auth.test.js`, `server/routes/settings.secretsStrip.test.js`)

- **Branch & PR reconciliation now runs on any managed app as a Chief-of-Staff scheduled task — not just PortOS.** The branch reconciler (finish this machine's unfinished local git work: delete fully-merged orphaned branches + their worktrees, open PRs for pushed-but-unopened work, resolve merge conflicts, drive the review loop, and auto-merge when a PR is green) used to be a PortOS-only feature with its own daily cron, `/api/branch-reconcile` route, `settings.branchReconcile` toggle, and a dedicated Settings tab — hardwired to the PortOS checkout. It is now a first-class per-app CoS task type, **`branch-reconcile`**, configured like every other improvement task (per-app enable, provider/model, cadence, cooldown, budget) under Chief of Staff → task scheduling. It runs `perpetual` (drain-until-done): each dispatch runs the deterministic, peer-safe cleanup + classification against the *app's* repo, dispatches the coordinator agent only while actionable in-flight branches remain, then parks on a daily recheck — so no agent is burned on a no-op run. A progress-signature convergence guard ensures the drain only re-dispatches when the actionable set actually *changed* since the last run (a branch advanced NEEDS_PR→IN_REVIEW→merged, or a new/removed branch): a cycle that leaves the same branches in the same states (a branch judged "not ready", a PR blocked on human review or red CI) parks instead of re-spawning an identical coordinator back-to-back. The peer-safety guarantee is unchanged (only local `refs/heads/` are ever touched; a federated peer's branch exists here only as a remote-tracking ref and is structurally invisible), and the per-behavior toggles (`cleanupMerged` / `openPr` / `resolveConflicts` / `autoMerge`) are now per-app task-metadata overrides. Off by default; enabling it on an app is the user's explicit consent to let it drive PRs on a schedule. The old dedicated scheduler, route, settings key, and Settings tab are removed; a migration (`scripts/migrations/170-branch-reconcile-to-cos-task.js`) carries a previously-enabled `branchReconcile` opt-in into the new task (cron→recheckCron, actions→per-app metadata) scoped to the PortOS app, and drops the dead settings key — so an install that had it on keeps reconciling instead of silently stopping. (`server/services/branchReconcile.js`, `server/services/cosTaskGenerator.js`, `server/services/taskSchedule.js`, `server/services/taskPromptDefaults/prompts.js`)

- **[issue-1929] Creative Director scene-0 render no longer races its seeded reference frame.** When first-pass gen is on (#1867), the treatment handler enqueues a fire-and-forget image-gen job per scene to seed `scene.sourceImageFile`; but `advanceAfterSceneSettled` fired the instant the treatment task settled and called `runSceneRender` for the lowest-order pending scene, which reads `sourceImageFile` synchronously — so scene-0 almost always rendered text-to-video before its just-queued seed frame landed, silently degrading the establishing shot. The "find next pending scene" branch now checks whether `project.generateFirstPass && !scene.sourceImageFile` and a `creativeDirector`-tagged image job for that scene is still queued/running on the media-job queue; if so it defers the render, re-firing `advanceAfterSceneSettled` once the seed job settles (with a short bounded poll for the scene-image hook to file `sourceImageFile`, since both handlers run off the same `completed` event). If the seed job fails/cancels the scene renders text-to-video exactly as the fire-and-forget contract always allowed. A 5-minute backstop timer force-proceeds if the seed job never emits a terminal event (worker crash / queue stall / restart that drops the in-memory job), so a stuck seed can't wedge the scene in `pending` forever; the backstopped re-advance renders the scene directly (via a `skipSeedDeferSceneId` gate) instead of re-arming a fresh defer on the same stalled job, and the settle listener matches by scene tag so a duplicate/re-queued seed job for the same scene wakes it too. Later scenes are unaffected (they already have a multi-minute buffer). (`server/services/creativeDirector/completionHook.js`)

- **[issue-2175] Generation-side character framework + structure doctrine (CWQE Phase 10 remainder).** Following the worldbuilding-doctrine slice (#2219), the character-generation and structure-planning prompts now teach the craft up front. The character bible gains the Ghost → Wound → Lie → Want → Need chain (with interlock checks: state the Lie in one sentence; the Need/Truth is its direct opposite; the Ghost causally explains the Lie), a declared arc type (positive/negative/flat), the Three Sliders (proactivity/likability/competence 1–10, rule: HIGH on ≥2 or one-with-growth; all-low = boring, all-high = Mary Sue), and a secrets list — all OPTIONAL so pre-#2175 records round-trip unchanged (`server/lib/storyBible.js` sanitizer + Zod-shape parity via the existing `canonArrayField` sanitize-on-write, client mirror in `client/src/lib/bibleLimits.js`, character-sheet UI in `CharacterDetailEditor.jsx` with `htmlFor`/`id` pairing, and no-clobber merge in `universeCharacterExpand.js` for the AI-expand path). The `character.consistency` / `arc.*` editorial checks now receive the authored Lie/Want/Need/arc-type when present (via `canonCharacterTraitsSummary`) so they reconcile plan-vs-delivery instead of inferring both. Structure prompts gain lean doctrine: `pipeline-season-episodes.md` a try-fail mandate (60%+ of middle episodes end "Yes, but"/"No, and"), beat rules (Catalyst external; Break Into Two a protagonist choice; All Is Lost includes a death — literal or of a hope/relationship/identity) and an active-climax rule; `pipeline-arc-overview.md` MICE-style thread nesting (threads close in reverse order of opening; name the opening/closing thread per season). Shipped-template edits ship to existing installs via hash-keyed migrations 171/172/173 (with the drift-cross-sync of prior owners 003/005/019/027/166). Remaining Phase 10 items (Le Guin style doctrine into the style-guide flow) stay tracked on #2175.

- **[issue-2160] Bulk-history importer for browser history → timeline `web.visit` events, on the shared import seam.** The `/timeline` page gains a collapsible "Import browser history" panel alongside the Spotify, location, Discord, and WhatsApp ones: request the **Chrome** product from Google Takeout (takeout.google.com), then upload the ZIP (or a single `History.json`) → preview parsed visit counts + unique sites + date-range + top domains → confirm, and every page visit lands as an idempotent `web.visit` event (source `browser`) via the existing `recordEvents()` infra. Chromium browsers have no in-app history export, so Takeout's `History.json` (or the older `BrowserHistory.json`) is the ingestion path; `time_usec` is epoch-microseconds (UTC, already normalized by Takeout) → divided to ms for the instant. The full URL lands in the row's `url` column while the summary keeps only the hostname (privacy contract — no query strings in the preview line). Subframe navigations (`AUTO_SUBFRAME`/`MANUAL_SUBFRAME` — ad/embed iframe loads Chrome records alongside real navigations) are dropped as noise, the same way the location importer drops travel segments. Since a visit carries no id in the export, the dedupe key is a stable content hash of (visit-instant + URL), so re-imports and overlapping exports never double-count. Built on the same shared seam: the generic `ActivityImportPanel`, the `importHandler` route factory, and the `importFile` client wrapper. (`server/services/browserHistoryImport.js`, `server/routes/timeline.js`, `client/src/services/apiTimeline.js`, `client/src/components/timeline/BrowserHistoryImportPanel.jsx`, `client/src/pages/Timeline.jsx`)

- **[issue-2160] Bulk-history importer for WhatsApp chat exports → timeline `message` events, on the shared import seam.** The `/timeline` page gains a collapsible "Import WhatsApp chat" panel alongside the Spotify, location, and Discord ones: in WhatsApp open a chat → ⋯ More → Export chat, then upload the `_chat.txt` (or the exported `.zip`) → preview parsed message counts + unique senders + chat title + date-range + top senders → confirm, and every message lands as an idempotent `message` event (source `whatsapp`) via the existing `recordEvents()` infra. Both export layouts parse: iOS bracketed lines (`[2024-01-15, 6:30:45 PM] Name: …`) and Android dash lines (`1/15/24, 6:30 PM - Name: …`), with multi-line message bodies folded back together, bidi marks stripped, system notices (encryption/group changes) skipped, and media-only lines kept with a placeholder. Because WhatsApp timestamps are LOCAL wall-clock with no offset, they're anchored to your configured timezone (reusing the calendar importer's `resolveEventInstant`) instead of the server's OS zone; the M/D vs D/M date order is auto-detected per file (a component >12 disambiguates; ambiguous files default to M/D). WhatsApp exports don't mark which sender is "you", so direction (sent vs received) is left neutral for now with the sender kept per event — a later reclassification pass, plus Gmail/YouTube/browser-history sources, stay tracked on #2160. Since WhatsApp lines carry no message id, the dedupe key is a stable content hash of (chat, instant, sender, body), so re-imports and overlapping exports never double-count. Built on the same shared seam: the generic `ActivityImportPanel`, the `importHandler` route factory, and the `importFile` client wrapper. (`server/services/whatsappImport.js`, `server/routes/timeline.js`, `server/lib/navManifest.js`, `client/src/services/apiTimeline.js`, `client/src/components/timeline/WhatsappImportPanel.jsx`, `client/src/pages/Timeline.jsx`)

- **[issue-2160] Bulk-history importer for Discord data packages → `message.sent` events, on the shared import seam.** The `/timeline` page gains a collapsible "Import Discord history" panel alongside the Spotify and location ones: request your Discord "data package" (Settings → Data & Privacy → Request all of my Data), then upload the ZIP (or a single `messages.json`/`.csv`) → preview parsed message counts + unique channels + date-range + top channels → confirm, and every message you sent lands as an idempotent `message.sent` event (source `discord`) via the existing `recordEvents()` infra. Because every message in the package is authored by the account owner and Discord message IDs are globally-unique snowflakes, the dedupe key is just the message id, so re-imports and overlapping exports never double-count. Both export vintages parse: newer `messages.json` and older `messages.csv` (a small in-tree RFC-4180 CSV reader — no new deps — handles quoted fields with embedded commas/quotes/newlines), each channel's `channel.json` is joined to give guild/DM context, offset-less CSV timestamps are anchored to UTC, and attachment-only messages are kept with a placeholder body. Built on the same shared seam: the generic `ActivityImportPanel`, the `importHandler` route factory, and the `importFile` client wrapper. (`server/services/discordImport.js`, `server/routes/timeline.js`, `client/src/services/apiTimeline.js`, `client/src/components/timeline/DiscordImportPanel.jsx`, `client/src/pages/Timeline.jsx`)

- **[issue-2160] Bulk-history importer for Google Takeout Location History → `place.visit` events, on the shared import seam.** The `/timeline` page gains a collapsible "Import Google location history" panel alongside the Spotify one: upload the Takeout "Location History (Timeline)" ZIP (or a single `Semantic Location History` JSON) → preview parsed visit/place counts + date-range + top places → confirm, and every place visit lands as an idempotent `place.visit` event (source `location`) via the existing `recordEvents()` infra (dedupe on place identity + visit start, so re-imports and overlapping exports never double-count). Both export vintages parse: classic server-side Timeline (`timelineObjects[].placeVisit` with E7 coords and ISO or epoch-ms durations) and the 2024+ on-device export (`semanticSegments[].visit.topCandidate` with `latLng` strings). Travel/activity segments carry no "where was I" signal and are dropped; the raw `Records.json` GPS dump is deliberately skipped. The two importers now share ONE seam: a generic `ActivityImportPanel` (pick → preview → confirm, stale-response-guarded) that Spotify + location wrap thinly, one `importHandler` route factory on `/api/timeline`, and one `importFile` client wrapper — so the remaining sources are small follow-ups. (`server/services/takeoutLocationImport.js`, `server/routes/timeline.js`, `client/src/services/apiTimeline.js`, `client/src/components/timeline/{ActivityImportPanel,TakeoutLocationImportPanel,SpotifyImportPanel}.jsx`, `client/src/pages/Timeline.jsx`)

- **[issue-2160] Bulk-history importer for Spotify extended streaming history + shared import seam.** The `/timeline` page gains a collapsible "Import Spotify history" panel: upload the privacy-download ZIP (or a single `Streaming_History_Audio_*.json`) → preview parsed counts/date-range/top-artists → confirm, and every play lands as an idempotent `media.listen` event via the existing `recordEvents()` infra (dedupe on track-uri + played-at, so re-imports and overlapping exports never double-count). Both the modern extended-history shape (`ts`/`ms_played`/`master_metadata_*`) and the legacy `StreamingHistory*.json` shape (`endTime`/`artistName`/`trackName`/`msPlayed`) are parsed; legacy offset-less UTC timestamps are anchored to UTC so a server west of UTC can't shift the listen into the wrong day. The route reuses the streaming `multipart.js` parser + `zipStream.js` (no new deps; the JSON parser is hand-written), Zod-validates the `preview` flag, and always unlinks the temp upload. This is one slice of the larger backfill issue. (`server/services/spotifyImport.js`, `server/routes/timeline.js`, `client/src/services/apiTimeline.js`, `client/src/components/timeline/SpotifyImportPanel.jsx`, `client/src/pages/Timeline.jsx`)

- **[issue-2264] Image Cleaner GPU FLUX diffusion round-trip + resolution-aware options + save-to-gallery.** Completes the composable Image Cleaner (predecessor #1763): the `/devtools/image-clean` page can now run the higher-reliability GPU FLUX img2img round-trip, not just the CPU light pass. The GPU render is GPU-serialized through `mediaJobQueue` (two concurrent FLUX renders OOM the box), so for `?diffusion=gpu` the route stages the already-sync-cleaned bytes to a non-gallery temp dir, enqueues a job through the SAME regen seam the lightbox `/regenerate` flow uses (`resolveRegenBackend` / `buildRegenParams` / `clampRegenDimensions` / `enqueueJob`), and returns HTTP 202 + a `jobId`; the client tracks progress via the existing media-job channel (`useMediaJobProgress`), then GETs the finished bytes from a result endpoint. A non-gallery render seam was factored out of `generateImage` (`resolveOutputPlacement` + an `outputTarget` param): the cleaner renders to `data/image-clean-tmp/` with **no sidecar** and the gallery `completed` index/federate hooks suppressed, so a clean pass never pollutes the gallery by default — the lightbox gallery-write + lineage/variant behavior is untouched. The finished result is ephemeral; an explicit "Save to gallery" action promotes it via `saveUploadedGalleryImage`. Resolution-aware diffusion options land in the page: a GPU/CPU sub-mode selector auto-picked by hardware (`getRegenAvailability()`), a strength slider bounded by the server's `strengthMin`/`strengthMax`, an optional max-render-MP override, a render-budget warning when the source exceeds the FLUX attention budget, and a resolution-squeeze note. The ignore-zone preserve-region mask (#1763) applies to the GPU sub-mode too — the original (oriented) pixels are composited back over the diffused frame after the render. A short-grace age-gated GC (`imageCleanTmpGc.js`) sweeps the temp working files. (`server/routes/imageClean.js`, `server/services/imageGen/cleanGpu.js`, `server/services/imageGen/local.js`, `server/services/imageGen/regen.js`, `server/services/imageCleanTmpGc.js`, `server/lib/fileUtils.js`, `client/src/pages/ImageClean.jsx`, `client/src/services/apiMedia.js`)

- **[issue-2262] Layered Intelligence Loop — deterministic backbone (server slice).** A perpetual, per-managed-app self-improvement loop that runs as an Engine-B autonomous script job (`job-layered-intelligence`, off by default). On its schedule the handler sweeps `getActiveApps()` and, for each app whose `layeredIntelligence.enabled === true` and whose per-app interval has elapsed, runs four layers: GATHER (reads enabled sources — GOALS.md, CoS `learning.json` metrics, HEALTH_REPORT.md, PLAN.md, open tracker issues, plus app-declared `file` custom sources), REASON (`callProviderAISimple` against the active/overridden provider with a JSON-only prompt), DECIDE (deterministic scope-gate + slug/label dedup against live tracker state), and ACT (files exactly ONE issue via the app's resolved work tracker, and optionally pauses the app on a blocking issue). The reasoning model never touches code — it returns structured JSON and every side effect is deterministic handler code, so the "model must not make direct code changes" contract holds by construction. Dedup and pause are fully tracker-derived (a `<!-- lil-slug: … -->` marker + `layered-intelligence` / `layered-intelligence:blocking` labels; closed-issue suppression pinned at 30 days) — the only local state is per-app `lastRunAt` bookkeeping. Meta/self proposal scopes (`loop-meta` / `portos-self`) are double-enforced to PortOS-only so a hallucinated or hand-edited scope can't file against the loop from someone else's app. GitHub (`gh`) and GitLab (`glab`) forge filing + `plan`-tracker PLAN.md append are implemented; ships via the `DEFAULT_JOBS` catalog (merged into existing installs on boot by `applyAdditiveFields`) and the per-app config accessor's default (all sources on; PortOS gets the meta/self scopes) — so no seed file or migration is needed. Off by default; only runs as a user-enabled scheduled automation (no cold-bootstrap LLM calls). (`server/services/layeredIntelligence.js`, `server/services/autonomousJobs/layeredIntelligenceHandler.js`, `server/services/autonomousJobs/scriptHandlers.js`, `server/services/autonomousJobs/defaults.js`, `server/services/apps.js`, `server/lib/validation.js`)

- **[issue-2262] Layered Intelligence Loop — per-app config UI (client slice).** The Edit App drawer gains an "Intelligence" tab that configures the self-improvement loop per managed app: enable/disable, a run-interval preset select (hourly → weekly, with a "Custom" readout for a hand-set cadence), an optional provider/model override (defaults to the active CoS provider), the Layer-1 telemetry source toggles (goals, CoS metrics, health report, PLAN.md, open issues), an add/remove list of repo-relative custom `file` sources, a free-text guidance-rules box, and the allowed-proposal-scope checkboxes — with the `loop-meta` / `portos-self` scopes shown only on the PortOS baseline app (mirroring the server's PORTOS-only gate). The tab reads the effective config from a new read-only `GET /api/apps/:id/layered-intelligence` endpoint (stored partial merged over the shipped defaults + `isPortos` flag) and, on save, PATCHes only the fields that changed against that baseline via a `buildLayeredIntelligenceUpdate` diff — so an unchanged config stays absent and the server's merge-over-stored contract keeps the install forward-compatible with future default changes (persisting the full effective config would freeze it). No migration or seed file is needed: the job ships via the `DEFAULT_JOBS` boot-merge and the per-app config comes from the accessor default (per the design doc in `docs/plans/2026-07-07-layered-intelligence-loop.md`). (`client/src/components/apps/LayeredIntelligenceTab.jsx`, `client/src/components/apps/EditAppDrawer.jsx`, `client/src/services/apiApps.js`, `server/routes/apps.js`)

- **[issue-2262] Layered Intelligence Loop now files to Jira (tracker abstraction completed).** A Jira-tracked managed app previously short-circuited BEFORE reasoning (`skipped: jira-filer-not-implemented`) — only GitHub/GitLab forges and PLAN.md apps could file. The loop's "act" step now files through the PortOS Jira REST service for any app whose resolved work tracker is `jira` with an enabled `jira.instanceId`/`projectKey` (Jira is never auto-detected from the git host, matching `resolveAppWorkTracker`). A Jira app with no usable config still skips before reasoning (`jira-not-configured`) so no LLM call is burned on a result that can't land. Dedup, park, and pause reuse the same tracker-derived model as the forges: the `<!-- lil-slug: … -->` marker is embedded in the ticket description and matched via a `labels = "layered-intelligence"` JQL scan (with the `ok`/`issues` failed-vs-empty sentinel so a transient Jira blip suppresses filing rather than duplicating), closed-issue suppression uses the ticket `resolutiondate` against the shared 30-day window, and pause applies a Jira-safe `layered-intelligence-blocking` label (colons/spaces stripped) filtered by `statusCategory != Done` so a resolved blocker un-parks the app automatically. Because Jira issues are keyed strings (`PROJ-123`), `pause.blockOnIssue: "this"` resolves to the just-filed ticket's key and an integer resolves to `<projectKey>-<n>`. Adds three generic Jira service helpers (`searchIssues`, `addLabels`, exported `escapeJql`) reused by the filer. (`server/services/jira.js`, `server/services/layeredIntelligence.js`, `server/services/autonomousJobs/layeredIntelligenceHandler.js`)

- **[issue-2277] Layered Intelligence custom sources gain `http` + `cmd` types (full-stack).** The Intelligence tab's custom-source list previously only offered `file` rows even though the design called for three source types — `http`/`cmd` sources could only be set through the API. Each row now has a type selector (File / URL / Command) that swaps in the type-appropriate value field (repo-relative `ref` / `url` / `cmd`) plus an optional `label`, and "Add source" seeds a new file row. The backend backbone the UI needs was also added in the same change (it didn't exist in `main` yet): the Zod schema is now a `type`-discriminated union (`file` keeps the repo-relative-path refine, `http` requires an http(s) URL, `cmd` is a bounded command string, all with an optional label), and `gatherSources` reads the new types via two injectable, degrade-to-omit helpers — `fetchHttpSource` (http(s)-scheme-guarded, 10s-timeout `fetch`, null on non-2xx/throw) and `runShellCommand` (shell-run in the app's repo dir, 15s timeout, null on non-zero exit). A `customSourceKey` helper namespaces the prompt map key by type so a `file` ref and an `http` url that share a string can't collide. Sanitize/diff on the client mirrors all three types + label so a blank/half-typed row still sanitizes away (no over-persist), and a type change clears the stale value field while keeping the label. (`server/lib/validation.js`, `server/services/layeredIntelligence.js`, `client/src/components/apps/LayeredIntelligenceTab.jsx`)
