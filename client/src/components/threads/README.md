# Threads Components

Affordances and components for Brain threads (#7664) — tracked open loops (bullet journal sense), distinct from message threads.

| Component | Purpose |
|---|---|
| `AddToThreadButton.jsx` | Compact button opening an `EntityCombobox` over open threads to attach PortOS records or external items. |
