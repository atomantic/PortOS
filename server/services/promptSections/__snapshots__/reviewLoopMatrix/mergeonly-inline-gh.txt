## Merge Gate
This runs as **step 4 of the Completion Workflow above**, against the PR you just opened on `feature/example` (`https://github.com/example-org/example-repo/pull/42` / `42` are the shell variables you captured there). **No code review was requested for this task, so nothing else will merge this PR Land it yourself once CI is green.**

1. **Wait for CI to finish**: `gh pr checks "https://github.com/example-org/example-repo/pull/42" --watch --fail-fast --interval 30`. "No checks reported" is AMBIGUOUS — a just-opened PR reports it while checks are still attaching, and merging on it races the CI this gate exists to wait for. Treat it as green ONLY when the repo genuinely has no CI (`gh workflow list` is empty / nothing in `.github/workflows` triggers on pull_request, and no external status check is configured). If CI IS expected, wait 30s and re-check for up to 5 minutes — and if it still hasn't attached, **leave the PR open and say so**; never merge on checks that were expected but never appeared.
2. **Clear whatever blocks the merge, then re-check.** If a check failed, read the failing job's log (`gh run view --log-failed`), fix the cause here, run the project's tests, commit (`fix:` prefix, no Co-Authored-By), push, and go back to the previous step — cap this at 5 rounds. If `gh pr view "https://github.com/example-org/example-repo/pull/42" --json mergeable -q .mergeable` reports `CONFLICTING`, `git fetch origin`, rebase onto the base branch, resolve the conflicts keeping BOTH sides' intent, re-run the tests, `git push --force-with-lease`, and re-check.
3. **Merge** with exactly these flags, nothing else — a true merge commit keeps the branch tip in the base branch's history so automated worktree cleanup can prove the branch is merged, and any merge-deferral flag leaves the PR open after you exit. If it is already merged, skip to the next step:
   ```bash
   gh pr merge "https://github.com/example-org/example-repo/pull/42" --merge --delete-branch
   ```
   If that is rejected because this repo disallows merge commits, re-check what it allows (`gh repo view --json mergeCommitAllowed,squashMergeAllowed,rebaseMergeAllowed`) and merge with an allowed method instead — `--squash` first, else `--rebase`, keeping `--delete-branch`.
4. **Confirm the merge before exiting**: `gh pr view "https://github.com/example-org/example-repo/pull/42" --json state -q .state` must return `MERGED`. If it is still open or was closed unmerged, investigate (failing check, merge conflict, branch protection), fix, and retry. Leave it open — saying so explicitly in your completion summary — if CI stays red after a genuine fix attempt, a conflict needs a human decision, expected checks never attached, or a branch protection you cannot satisfy blocks the merge (a required approving review, a required check only a human can trigger). Hand those to a human rather than retrying until you time out.
5. Return to the **Completion Workflow** above and write the completion sentinel — the run is not done until you have. Do NOT open a second PR. Do NOT start a code review — none is configured for this task.

**Hard stop:** if CI is still red after 5 fix rounds, or a conflict needs a product decision you can't make, post a PR comment summarising exactly what is blocking the merge and exit with the PR left open. Do not force a merge over red CI.